# FBZ API 后端长期执行 Goal

本文用于把当前 Codex 线程里的长期 goal 固化到仓库。换电脑继续开发时，先让 Codex 读取本文，再继续分步执行。

## 当前长期 Goal

分步开始完成计划，每一个控制器、函数、方法都需要考虑拓展性，避免不必要的拆分，合理规划项目结构，SQL 安全，性能安全；本地有 Docker，可以启动需要的 Redis 和数据库。

## 项目定位

- `fbz-api` 是类 Emby 的 Rust API 后端，目标是让支持 Emby 协议的客户端填写本项目地址后可以连接和使用。
- 当前后端优先做 Web/API 后端架构和协议兼容，前端管理页后续再接。
- 架构形态优先保持 modular Rust monolith，使用 `axum + tokio + tower-http + tracing`，逐步扩展模块边界。
- 基础依赖是 PostgreSQL 和 Redis；Docker 当前主要用于本地依赖和后续生产部署验证。
- 目标部署环境包括 Windows、Linux、Docker、NAS。

## 已确认硬约束

- Emby-only compatibility：优先兼容 Emby REST API 和常见客户端行为。
- v1 支持 XML 请求体解析，但响应仍以当前 JSON DTO 为主。
- PostgreSQL / Redis 是基础依赖。
- `FFMPEG_PATH=ffmpeg`、`FFPROBE_PATH=ffprobe` 作为默认值，并支持外部覆盖。
- 支持 TMDB / TVDB / Fanart 元数据源，API base URL 可由管理员替换为镜像。
- 支持系统 HTTP 代理配置。
- STRM 默认只允许内网链接；公网域名必须通过安全域名 allowlist 配置。
- 支持硬件转码，默认硬解，不可用时回落软件转码。
- 转码默认最多 3 个并发，其余排队。
- 插件系统需要支持外部插件、hook、计划任务、Host API、通知插件、后台菜单，但必须约束插件边界。
- 通知目标包括 Telegram、企业微信和通用 webhook，但敏感目标配置由管理员管理，插件只提交受控通知请求。
- 多用户权限包括服务器管理权限、媒体库权限、下载/转码权限、新设备登录权限和 Emby 用户策略映射。
- 预估规模：最多约 1000 人同时使用；媒体量可能达到 PB 级、电影/电视剧/音乐条目几十万到百万级。

## 执行原则

- 先读当前代码和文档，再修改。
- 每轮只推进一个清晰方向，完成后运行验证。
- 不为“未来可能需要”过度拆分；只有重复、复杂度或边界真实存在时再抽象。
- 控制器/路由层只做认证、参数解析、DTO 映射和错误转换；业务和 SQL 边界放到 service/repository。
- Repository 查询必须做权限过滤，不能让上层靠事后过滤兜底。
- 所有用户输入进入 SQL 必须走 bind 参数、枚举 allowlist 或规范化 filter，不拼接 SQL 文本。
- 大表和高增长审计表优先 keyset pagination，避免 offset 扫描。
- 对可能很大的列表，不要把全量 ID 拉回应用层再过滤。
- 迁移文件可以新增；执行生产或真实数据库结构变更前要明确确认。
- 默认不做 git commit、push、reset、checkout，除非用户明确要求。

## 另一台电脑继续执行步骤

1. 拉取或同步仓库后进入后端目录：

```powershell
cd C:\Code\fbz\fbz-api
```

2. 让 Codex 先读取这些文件：

```text
docs/plans/backend-execution-goal.md
README.md
docs/database-scale.md
docs/plugin-system.md
docs/plugin-development.md
```

3. 先检查当前工作树和依赖状态：

```powershell
git status --short
cargo test --quiet
cargo build --quiet
```

4. 如果需要本地依赖，可以启动 PostgreSQL 和 Redis。当前开发依赖优先用 `./scripts/dev-deps.ps1`；脚本会调用 `docker-compose.dev.yml` 并等待 Postgres/Redis healthy。如果脚本不可用，再按 `.env.example` 的 `DATABASE_URL` / `REDIS_URL` 启动本地服务。

5. 每轮修改后至少运行：

```powershell
cargo fmt
cargo test --quiet
cargo build --quiet
cargo fmt -- --check
git diff --check
```

涉及 PowerShell 脚本时，再运行相关脚本测试，例如：

```powershell
./scripts/dev-deps.test.ps1
./scripts/smoke-plugin-signature.test.ps1
```

涉及 HTTP 插件 helper 或示例时，再运行：

```powershell
node --test examples/plugins/_shared/fbz-plugin-http.test.mjs
node --test examples/plugins/first-party-notifier-templates.test.mjs
node --check examples/plugins/_shared/fbz-plugin-http.mjs
node --check examples/plugins/http-notification-bridge/server.mjs
node --check examples/plugins/http-marker-importer/server.mjs
node --check examples/plugins/telegram-notifier-template/server.mjs
node --check examples/plugins/wecom-notifier-template/server.mjs
node --check examples/plugins/webhook-notifier-template/server.mjs
```

## 当前已推进的方向

- 基础 Rust 后端骨架：健康检查、配置、日志、优雅关闭。
- Emby 兼容路由逐步扩展：系统信息、认证、用户、设备、媒体库、Items、Genres、Artists、Persons、播放状态、图片、下载、PlaybackInfo、转码、音乐相关入口等。
- Emby 启动流程已补 `Users/{Id}/Views` 和 `UserViews?UserId=...` 媒体库视图入口，`UserViews` query 字段兼容 PascalCase、lower-camel 和 snake_case；`Users/{Id}/GroupingOptions` 已补官方 no-auth 分组探测入口，路径用户 id 受控规范化后返回空 `BaseItemDto[]`，避免真实分组偏好模型落地前客户端启动探测 404 或泄露媒体库结构。
- Emby 系统探活和版本说明已补 `System/Ping` GET/HEAD/POST 空成功响应，以及 `System/ReleaseNotes` / `System/ReleaseNotes/Versions` 认证读取入口，返回当前 FBZ API 版本的受控 `PackageVersionInfo` 记录，避免启动探测或更新说明探测落到 404。
- Emby 服务器日志管理探测已补 `System/Logs` 和 `System/Logs/Log`；FBZ 输出结构化 stdout/stderr 而非滚动磁盘日志文件，`System/Logs` 按管理员权限返回空 `LogFile[]`（DTO 形态与官方一致），`System/Logs/Log?name=...` 先对 `name` 做必填、长度上限（256）、路径分隔符/`..` 穿越和控制字符安全校验（畸形返回 422），再返回受控 not found，避免管理页日志查看器探测 404 或客户端通过日志读取入口探测主机文件系统路径。加 `system_logs_routes_are_registered_with_prefixed_and_plain_paths` 路由存在测试和 `log_name_accepts_bounded_path_safe_values` / `log_name_rejects_empty_and_unsafe_values` 校验测试。
- Emby 系统配置服务已补 `System/Configuration/{Key}`、`POST System/Configuration` 和 `POST System/Configuration/Partial`；命名读取按认证用户返回受控系统/metadata/transcoding/branding 配置切片，写入口按管理员权限和体积上限校验后返回受控冲突，避免真实运行配置持久化模型外制造未落库成功状态。
- Emby 系统电源控制探测已补 `POST System/Restart` 和 `POST System/Shutdown`；入口按管理员权限保护，FBZ 进程生命周期由部署的容器/服务管理器接管（按信号优雅关闭），两个入口返回受控冲突而非让 Emby 客户端越界重启或停止进程，避免管理页电源按钮探测 404 或制造越界停机。加 `system_power_control_routes_are_registered_with_prefixed_and_plain_paths` 路由存在测试。
- Emby 管理页功能探测已补 `Features`，按管理员权限返回静态 `FeatureInfo[]`，暴露核心后端和 Emby 兼容系统能力，后续可接真实 feature/license/plugin feature 聚合。
- Emby 插件管理探测已补 `Plugins`、`Plugins/{Id}/Configuration`、`Plugins/{Id}/Thumb`、`DELETE Plugins/{Id}` 和 `Plugins/{Id}/Delete`；列表按管理员权限映射 FBZ 插件摘要，配置读取返回内部插件配置值，写配置和卸载暂保持受控冲突响应，避免绕过 FBZ 插件审批、签名和启停生命周期。
- Emby 包管理探测已补 `Packages`、`Packages/{Name}`、`Packages/Updates`、`Packages/Installed/{Name}`、`Packages/Installing/{Id}` 和 `Packages/Installing/{Id}/Delete`；包目录按认证用户返回受控 FBZ Core 系统包并支持官方 `PackageType`、`TargetSystems`、`IsPremium` 和 `IsAdult` 过滤，query 字段兼容 lower-camel/snake_case，更新列表返回空数组，安装入口保持受控冲突响应并指向 FBZ 签名插件包链路，取消安装为认证管理员 no-op。
- Emby DLNA profile 管理探测已补 `Dlna/ProfileInfos`、`Dlna/Profiles/Default`、`Dlna/Profiles/{Id}`、`POST Dlna/Profiles`、`POST Dlna/Profiles/{Id}` 和 `DELETE Dlna/Profiles/{Id}`；读取入口按管理员权限返回受控 FBZ 默认 DLNA profile，写入 body 的常见 profile/identification/direct-play/transcoding/response/subtitle 字段兼容 lower-camel/snake_case，写入和删除保持受控冲突响应，避免真实 profile 持久化、设备识别规则和 DLNA 转码策略未设计完成前制造未落库成功状态。
- Emby 环境/目录浏览探测已补 `Environment/DefaultDirectoryBrowser`、`Environment/Drives`、`Environment/DirectoryContents`、`Environment/ParentPath`、`Environment/NetworkDevices`、`Environment/NetworkShares` 和 `Environment/ValidatePath`；入口按管理员权限保护，query/body 字段兼容 lower-camel/snake_case，目录浏览只读且单次最多返回 1000 项，网络发现暂返回空数组，路径验证只检查存在性、文件/目录类型和只读属性，不创建探针文件。
- Emby 编码设置探测已补 `Encoding/CodecConfiguration/Defaults`、`Encoding/CodecInformation/Video`、`Encoding/ToneMapOptions`、`Encoding/FullToneMapOptions`、`Encoding/PublicToneMapOptions`、`Encoding/CodecParameters` 和 `Encoding/SubtitleOptions`；`CodecParameters` query 字段兼容 lower-camel/snake_case，读入口按官方认证边界返回空/默认 DTO，写入口按管理员权限和体积上限校验后返回受控冲突，避免真实转码配置模型、硬件检测和字幕设置持久化未设计完成前制造未落库成功状态。
- Emby 活动日志管理探测已补 `System/ActivityLog/Entries`；入口按管理员权限保护，受控解析 `StartIndex`、`Limit` 和 `MinDate`，钳制病态超大 `StartIndex`，并兼容 lower-camel/snake_case query 字段，当前返回空 `QueryResult<ActivityLogEntry>`，后续可接真实活动日志 repository 和 keyset 查询。
- Emby 通知管理探测已补 `Notifications/Types`、`Notifications/Admin`、`Notifications/Services/Defaults` 和 `Notifications/Services/Test`；类型查询按认证用户返回空分类，默认服务返回禁用的 FBZ Host Notifications，管理员通知和服务测试入口按管理员权限解析官方字段并兼容 lower-camel/snake_case（含服务测试 body 的 notifier/service/user/library/event/options 字段）后返回受控冲突，避免真实投递模型外绕过 FBZ 通知目标审批与审计。
- Emby 本地化启动/设置探测已补 `Localization/Countries`、`Localization/Cultures`、`Localization/Options` 和 `Localization/ParentalRatings`，按认证用户边界返回常见国家、语言和分级静态 DTO，后续可接真实多语言/分级配置。
- Emby Live TV 探测已补 `LiveTv/Info` / `GuideInfo` 禁用信息，`LiveTv/Folder` 禁用顶层直播文件夹，`Channels` / `Programs` / `Programs/Recommended` / `RecommendedPrograms` / `UpcomingPrograms` / `EPG` / `ChannelTags` / `ChannelTags/Prefixes` / `ChannelMappingOptions` / `ChannelMappings` / `ListingProviders` / `ListingProviders/Available` / `ListingProviders/Default` / `ListingProviders/Lineups` / `Manage/Channels` / `TunerHosts` / `TunerHosts/Default/{Type}` / `TunerHosts/Types` / `Tuners/Discover` / `Tuners/Discvover` / `Recordings` / `Recordings/Folders` / `Recordings/Series` / `Timers` / `Timers/Defaults` / `SeriesTimers` 空列表或受控默认 DTO，并钳制病态超大 `StartIndex`，`Timers/Defaults` 兼容 `ProgramId`/`programId`，`POST LiveTv/Programs` 兼容空 EPG 查询，以及 `LiveTv/Programs/{Id}` / `Channels/{Id}` / `Recordings/{Id}` / timer 详情受控 not found；直播源、频道映射、节目单 provider、频道管理、录制、timer、series timer、tuner host 和 tuner reset 写/删入口返回受控冲突，避免未接真实直播源模型前客户端能力和节目详情探测走缺省 404 或制造未落库成功状态。
- Emby 首页内容服务已补 `Users/{Id}/HomeSections` 和 `Users/{Id}/Sections/{SectionId}/Items`；section 列表按认证用户返回 latest/resume/favorites/library 常见分区，分区 items 通过 section allowlist 映射回已有用户可见 Items 查询，query 字段兼容 lower-camel/snake_case，继续由 repository 权限过滤兜住媒体库边界。
- Emby 首页「即将播出」栏目已补 `Shows/Upcoming`（`/emby` 与 plain 双路径），新增 `LibraryRepository::list_upcoming_episodes` 真实查询：按 `library_permissions.can_view` + 非隐藏库权限过滤 `item_type='episode'` 且 `premiere_date > current_date` 的剧集，权限过滤在仓储查询内完成（不靠上层事后过滤），按首播日期升序再按 season/episode 序号排序，支持可选 `ParentId` 媒体库范围和 offset 窗口，复用 `browse_result_lower_bound_from_rows` 做 lower-bound keyset 计数（避免 `count(*) over()` 全表扫描）；`UpcomingQuery` 受控解析 `UserId`/`ParentId`/`StartIndex`/`Limit`/`Fields`，客户端排序字段被解析但固定为首播日期升序，并兼容 lower-camel/snake_case query 字段。加 `shows_upcoming_routes_are_registered_*` 路由存在测试、`upcoming_query_parses_*` / `upcoming_window_*` 查询映射测试，以及 `upcoming_query_filters_future_premiere_with_safe_uuid_and_keyset_shape` SQL 结构守卫测试（断言 `premiere_date > current_date` 过滤、仓储侧权限过滤、安全 uuid 比较、premiere 升序、lower-bound keyset）；并新增 `#[ignore]` 实跑冒烟 `upcoming_query_executes_against_live_schema`（连真实 PG + 跑全量迁移 + 执行查询的两条分支，含 ParentId 库范围分支）。本地 dockerized PostgreSQL **已实跑通过**：`cargo test -- --ignored upcoming_query_executes_against_live_schema` 绿，证明该 SELECT 在真实 0001–0069 迁移 schema 上 parse/plan/执行均正确、premiere/权限/排序谓词被规划器接受。`cargo test --lib` 794 通过（3 ignored = WASI + 两条实跑冒烟），README 当前能力同步。
- Emby 首页兼容入口已补 `Users/{Id}/Suggestions`，复用权限过滤列表查询返回最近加入推荐窗口。
- Emby 首页两大高频栏目已补查询形入口,与既有路径形并存(很多客户端用 query 形)：最近加入 `Items/Latest?UserId=...`(对应 `Users/{Id}/Items/Latest`)和继续观看 `Items/Resume?UserId=...`(对应 `Users/{Id}/Items/Resume`)。各抽出 `latest_items_response` / `resume_items_response` 共享逻辑,query 形经 `authenticate_query_user` 鉴权,复用同一套权限过滤、排序和分页;`MediaListQuery` 补 `UserId` 字段并兼容 `userId`、`parentId`、`includeItemTypes`、`sortBy`、`fields` 等 lower-camel/snake_case 查询字段;`emby_items_latest_query_user_alias_exists` 集成测试覆盖 PascalCase 与 lower-camel URI;README 当前能力同步。
- Emby 音乐即时混音已把 `MusicGenres/{Name}/InstantMix` 从兼容空结果升级为真实流派种子混音：路径流派名作为权威种子（覆盖客户端传入的流派过滤，空名返回空结果），复用 `Songs?Genres=...` 的 `list_items_for_authenticated_user` Audio 查询链路，权限过滤与 DTO 映射与既有歌曲列表一致。
- Emby 音乐即时混音的 bare `Artists/InstantMix?Id=`（艺术家 `public_id` 种子）和 `MusicGenres/InstantMix?Id=`（流派 id 种子）也已从兼容空结果升级为真实混音，复用 `artist_ids` / `genre_ids` 权限过滤 Audio 查询（缺省/空种子返回空结果）；至此原 `instant_mix::empty_instant_mix` 空桩模块已全部被真实处理器取代并移除。
- Emby 项目种子即时混音 `Songs/{Id}/InstantMix`、`Albums/{Id}/InstantMix`、`Items/{Id}/InstantMix` 已从「同库同类型相似内容」回退升级为真实**流派种子音频混音**：新增 `LibraryRepository::list_instant_mix_seed_genre_ids`，在仓储查询内做权限过滤（granted library + `can_view` + 非隐藏库 + 未删除）解析种子项 `media_item_genres` 关联流派 id，用与全局一致的 `case ... uuid` 索引友好比较（恶意/畸形 id 匹配空，不 `public_id::text` 破坏索引）；返回 `Option<Vec<i64>>` 区分「种子不可见 → 404」与「可见但无流派 → 空混音」（后者复用 `find_user_item_by_id` 可见性判定，避免再退化为不相关列表）。新增 handler `item_instant_mix` 用解析出的流派 id 构建 Audio 查询（`include_item_types=Audio`、`media_types=Audio`、`recursive`、`genre_ids` 逗号拼接经 `id_list_filter` 还原为数字 id 集），并把种子项自身经 `exclude_item_ids` 排除，复用 `list_items_for_authenticated_user` 的同一套权限过滤与 DTO 映射；`mod.rs` 六条 InstantMix 路由（`/emby` + plain）从 `items::similar_items` 切到 `items::item_instant_mix`，`/Similar` 兄弟路由保持 `similar_items`（合集相似仍走相似内容）；`Playlists/{Id}/InstantMix` 维持返回播放列表自身曲目不变。加查询映射测试（`item_seed_instant_mix_filters_audio_by_seed_genres_and_excludes_seed`、无流派返回空查询）、SQL 结构守卫（`instant_mix_seed_genre_query_filters_visible_seed_genres`，断言流派 join、仓储侧权限过滤、安全 uuid 比较、可见性区分）、路由存在测试（`item_seed_instant_mix_routes_resolve_real_genre_seeded_audio`），以及 `#[ignore]` 实跑冒烟 `instant_mix_seed_genre_ids_executes_against_live_schema`。本地 dockerized PostgreSQL **已实跑通过**：连真实 0001–0069 迁移 schema 执行（不可见种子返回 None、畸形 id 解析为无种子均正确，证明流派 join 与全部权限谓词被规划器接受）。`cargo test --lib` 798 通过（4 ignored），README 当前能力同步。
- Emby 艺术家路径种子即时混音已补 `Artists/{Id}/InstantMix`（`/emby` + plain），作为 `Artists/InstantMix?Id=` 的路径形别名，路径 `artist_id` 作为权威种子并覆盖 query `Id`，继续复用 `artist_ids` 权限过滤 Audio 查询链路；新增 `artist_path_seed_instant_mix_uses_path_id_over_query_id` 查询映射测试和 `bare_artist_and_genre_instant_mix_resolve_seeded_audio_by_id` 路由覆盖，`cargo test instant_mix --lib --quiet` 通过（13 passed, 1 ignored）。
- Emby 专辑详情页歌曲入口已补 `Albums/{Id}/Songs`（`/emby` + plain），路径 `album_id` 作为权威 `album_ids` 过滤并覆盖 query `Albums` / `AlbumIds`，复用现有 `Songs` Audio 查询链路和 repository 权限过滤；新增 `album_path_songs_query_uses_path_album_id_over_query_filters` 查询映射测试和 `music_collection_routes_are_registered_with_prefixed_and_plain_paths` 路由覆盖，`cargo test music_ --lib --quiet`、`cargo test album_path_songs --lib --quiet` 通过。
- Emby 艺术家详情页歌曲/专辑入口已补 `Artists/{Id}/Songs` 和 `Artists/{Id}/Albums`（`/emby` + plain），路径 `artist_id` 作为权威 `artist_ids` 过滤并覆盖 query `Artists` / `ArtistIds`，分别复用现有 `Songs` Audio 查询链路和 `Albums` MusicAlbum 查询链路，repository 权限过滤、分页、图片字段和 DTO 映射保持一致；新增 `artist_path_songs_query_uses_path_artist_id_over_query_filters`、`artist_path_albums_query_uses_path_artist_id_over_query_filters` 查询映射测试，`music_collection_routes_are_registered_with_prefixed_and_plain_paths` 路由覆盖，以及 `emby_artist_music_collection_aliases_exist` router 测试。
- Emby MoviesService 推荐入口已补 `Movies/Recommendations`，按认证用户边界返回最近加入电影推荐分类并受控解析 `UserId`、`CategoryLimit`、`ItemLimit` 和图片字段，query 字段兼容 lower-camel/snake_case；`Movies/{Id}/Similar` 复用现有相似内容查询，常见 query 字段兼容 lower-camel/snake_case，避免电影详情页推荐探测 404/422。
- Emby 旧搜索提示入口已补 `Search/Hints`，复用权限过滤 Items 查询和 `SearchTerm` / `IncludeItemTypes` / `MediaTypes` 解析返回 legacy `SearchHints`。
- Emby 媒体库物理路径探测已补 `Library/PhysicalPaths`，按管理员权限返回启用中的媒体库路径数组，并在路由层规范化空路径和重复路径，避免普通用户侧媒体库浏览接口扩大为全局路径泄露。
- Emby 媒体库选项探测已补 `Libraries/AvailableOptions`，按认证用户返回受控默认 `LibraryOptionsResult`，包含 FBZ Metadata/Artwork、movies/tvshows/music/mixed 类型选项、默认图片选项和保守库默认配置，后续可接真实 provider/plugin 聚合。
- Emby 媒体库刷新入口已补 `POST Library/Refresh`，按管理员权限触发现有 `core.library.incremental_scan` 计划任务，复用调度租约、并发限制和扫描 job 入队链路，并保持官方 200 空响应形态。
- Emby 单项元数据刷新入口已补 `POST Items/{Id}/Refresh`，按服务器管理员权限解析官方 `Recursive`、`MetadataRefreshMode`、`ImageRefreshMode`、替换标记和 `BaseRefreshRequest` 字段，复用现有 `metadata.refresh` job 入队链路，并保持官方 200 空响应形态。
- Emby ImageService 图片信息入口已补 `Items/{Id}/Images`，按当前用户媒体库权限返回 `ImageInfo[]`，并将内部 artwork 类型映射为官方 `ImageType` / 同类型 `ImageIndex`；`Items/{Id}/Images/{Type}`、`Items/{Id}/Images/{Type}/{Index}` 与官方长缓存 URL `Items/{Id}/Images/{Type}/{Index}/{Tag}/{Format}/{MaxWidth}/{MaxHeight}/{PercentPlayed}/{UnplayedCount}` 继续复用现有本地 artwork / 安全远端图片读取边界，长路径 tag/format/尺寸/播放进度字段只做安全规范化后作为缓存参数忽略，避免客户端图片 URL 命中缺省 404；官方命名实体/用户图片别名 `Artists/{Name}/Images/{Type}`、`Genres/{Name}/Images/{Type}`、`MusicGenres/{Name}/Images/{Type}`、`Persons/{Name}/Images/{Type}`、`Studios/{Name}/Images/{Type}`、`Users/{Id}/Images/{Type}` 及各自 `{Index}` 形态已补 `/emby` + plain 双路径，路由层先做认证、路径 name/id/type/index 规范化和当前用户可见性校验，独立实体/用户头像图库未接入前返回受控 not found；官方全局图片入口 `Images/General`、`Images/General/{Name}/{Type}`、`Images/MediaInfo`、`Images/MediaInfo/{Theme}/{Name}`、`Images/Ratings` 和 `Images/Ratings/{Theme}/{Name}` 已补 `/emby` + plain 双路径，catalog 返回空数组，detail 返回受控 not found，避免静态资源/主题包未接入前客户端图片探测落到缺省路由 404；`POST/DELETE Items/{Id}/Images/{Type}`、`POST/DELETE Items/{Id}/Images/{Type}/{Index}`、`Images/{Type}/Delete`、`Images/{Type}/{Index}/Delete`、`Images/{Type}/{Index}/Index` 和 `Images/{Type}/{Index}/Url` 写入口探测已补管理员保护和字段规范化，当前返回受控冲突，避免真实 artwork 写模型、缓存落盘和元数据审计未设计完成前制造未落库成功状态。
- Emby RemoteImageService 已补 `Items/{Id}/RemoteImages`、`Items/{Id}/RemoteImages/Providers`、`Items/{Id}/RemoteImages/Download` 和 `Images/Remote`；读入口按认证用户和媒体库可见权限返回空远程图片结果与受控 provider 列表，并钳制病态超大 `StartIndex`，下载/代理入口按管理员权限解析官方字段后返回受控冲突，避免真实 artwork provider、缓存和元数据写链路未设计完成前制造假成功或绕过权限。
- Emby ItemLookupService / ItemUpdateService 已补 `Items/{Id}/ExternalIdInfos`、`Items/{Id}/MetadataEditor`、`Items/RemoteSearch/Image`、`Items/Metadata/Reset`、`Items/RemoteSearch/Apply/{Id}` 和类型化 `Items/RemoteSearch/{Book|BoxSet|Game|Movie|MusicAlbum|MusicArtist|MusicVideo|Person|Series|Trailer}`；外部 ID 信息返回 TMDB/TVDB/IMDb 受控 identifier 列表，metadata editor 按服务器管理员权限返回受控 `MetadataEditorInfo`（分级、国家、语言和外部 ID 选项），类型化远程搜索和应用结果 body 字段兼容 PascalCase、lower-camel 和 snake_case，按认证用户解析后返回空 `RemoteSearchResult[]`，图片代理、应用搜索结果和元数据重置入口按管理员权限解析官方字段后返回受控冲突，避免真实 provider 搜索、图片缓存和元数据重置链路未设计完成前制造未落库成功状态。
- Emby 媒体库结构读取已补 `Library/VirtualFolders` 和 `Library/VirtualFolders/Query`，复用当前用户可见媒体库权限过滤，返回 `VirtualFolderInfo` 的 `Locations`、`LibraryOptions.PathInfos`、`ItemId/Id/Guid` 和刷新状态，并对 Query 入口支持 `StartIndex` / `Limit` 兼容分页，同时钳制病态超大 `StartIndex`。
- Emby 在线频道探测已补 `Channels`，按认证用户边界返回空 `QueryResult<BaseItemDto>`，并受控解析 `UserId`、`StartIndex` 和 `Limit`，钳制病态超大 `StartIndex`，后续可接真实 channel/provider 聚合。
- Emby 用户数据读取/写入已补 `Users/{Id}/Items/{ItemId}/UserData`，复用权限过滤目标查询返回或更新播放进度、播放次数、收藏、评分和已看状态，写入 body/query 兼容 PascalCase、lower-camel 和 snake_case（`PlaybackPositionTicks`/`playbackPositionTicks`、`PlayCount`/`playCount`、`IsFavorite`/`isFavorite` 等）；body 为空时可直接从 query string 更新，body 与 query 同时存在时 body 字段优先；`Items/{Id}/Rating` 兼容 `Likes`/`likes`，`Users/{Id}/Items/{ItemId}/HideFromResume` 已补 `Hide=true/false` 与 `hide=true/false` 兼容入口，当前以受权限保护的播放位置清零方式隐藏继续观看项。
- Emby UserLibraryService 共享/访问写入口已补 `Items/Access`、`Items/{Id}/MakePrivate`、`Items/{Id}/MakePublic` 和 `Items/Shared/Leave`，按认证用户边界解析官方 item/user/access payload 后返回受控冲突，避免真实 item share 权限模型未设计完成前客户端探测 404/405 或制造未落库成功状态。
- Emby item / playback media source DTO 已补兼容空 `Chapters` 字段，`Fields=Chapters` 会被解析，后续可接真实章节扫描入库。
- Emby item / playback `MediaSourceInfo` 已补 `Type`、`Name`、`ItemId`、remote/open/probe/native-framerate 标记和默认音轨/字幕轨索引等基础兼容字段，便于真实客户端在 PlaybackInfo 后判断直连和播放面板状态。
- Emby 详情页兼容已补 `Items/{Id}/SpecialFeatures` 和 `Users/{Id}/Items/{Id}/SpecialFeatures`，按当前用户可见权限返回兼容空 special features 列表，query 字段兼容 lower-camel/snake_case，为后续真实 special feature 入库保留边界。
- Emby 播放前 extras 探测已补 `Items/{Id}/Intros`、`Users/{Id}/Items/{Id}/Intros`、`Items/{Id}/LocalTrailers` 和 `Users/{Id}/Items/{Id}/LocalTrailers`，按当前用户可见权限返回兼容空 intros/local trailers 边界，query 字段兼容 lower-camel/snake_case。
- Emby 删除菜单探测已补 `Items/{Id}/DeleteInfo`，按当前用户可见权限返回空 `Paths`，避免客户端探测 404，同时不暴露本地文件系统路径、不启用真实删除流程。
- Emby 详情页影评探测已补 `Items/{Id}/CriticReviews`，按当前用户可见权限返回空 `QueryResult<BaseItemDto>`，并受控解析 `StartIndex`、`Limit` 和 `UserId`，query 字段兼容 lower-camel/snake_case，后续可接真实外部影评 provider 或元数据缓存。
- Emby Trailers 聚合探测已补 `Trailers`，按认证用户边界返回空 `QueryResult<BaseItemDto>`，并受控解析 `UserId`、分页窗口、父级、搜索、类型、媒体类型和图片字段，query 字段兼容 lower-camel/snake_case，避免真实 trailer provider / trailer item 模型未接入前客户端探测 404/422 或伪造电影结果。
- Emby SyncService 探测已补 `Sync/Options`、`Sync/Targets`、`Sync/Jobs`、`Sync/Jobs/{Id}`、`Sync/JobItems`、`Sync/Items/Ready`、`Sync/JobItems/{Id}/AdditionalFiles` 和 `Sync/JobItems/{Id}/File` / `HEAD` 读入口，按认证用户边界返回官方空 dialog/list/query、受控 not found 或受控冲突形态，并受控解析 `UserId`、`TargetId`、`ItemIds`、文件名、分页和分类字段，钳制病态超大 `StartIndex`；`Sync/{TargetId}/Items`、`Sync/Items/Cancel`、`Sync/Jobs/{Id}` 和 `Sync/JobItems/{Id}` 取消/删除入口为认证 no-op，创建/更新同步任务、状态上报、离线动作和 job item 状态变更返回受控冲突，避免真实离线同步任务模型未设计完成前客户端探测 404/405 或制造假同步状态。
- Emby 播放队列探测已补 `Sessions/PlayQueue` 兼容空队列边界，受控解析 `Id` / `DeviceId` 查询范围并兼容 lower-camel/snake_case query 字段，后续可接真实 client dispatcher。
- Emby CollectionService 写入口探测已补 `POST Collections`、`POST/DELETE Collections/{Id}/Items` 和 `Collections/{Id}/Items/Delete`；入口按认证用户边界解析官方 `Name`、`IsLocked`、`ParentId` 和逗号分隔 `Ids`，并兼容 lower-camel/snake_case query 字段，当前返回受控冲突，避免真实合集模型、权限审计和成员写入未设计完成前制造未落库成功状态。
- Emby 播放列表写入口探测已补 `POST Playlists`、`Playlists/{Id}/AddToPlaylistInfo`、`POST/DELETE Playlists/{Id}/Items`、`Playlists/{Id}/Items/Delete` 和 `Playlists/{Id}/Items/{ItemId}/Move/{NewIndex}`，当前按认证用户边界和官方查询字段做安全解析，并兼容 lower-camel/snake_case query 字段；写动作保持受控冲突响应，避免真实播放列表写模型、审计和权限更新未设计完成前制造未落库成功状态。
- Emby Dynamic HLS 已补官方 `/Videos/{Id}/master.m3u8` 形态、视频/音频 `master.m3u8` / `main.m3u8` / `live.m3u8` manifest 入口、音频 lowercase `/audio` 别名、视频 `hls/{PlaylistId}/{SegmentId}.{SegmentContainer}` / `hls1/{...}` 与音频 `hls1/{PlaylistId}/{SegmentId}.{SegmentContainer}` segment 入口，以及 `subtitles.m3u8` / `live_subtitles.m3u8` 空字幕播放列表边界，复用转码 session 鉴权、用户可见权限和输出目录安全读取；HLS 文件/segment 与视频文件名流入口兼容 `TranscodeSessionId`/`transcodeSessionId` 和 `MediaSourceId`/`mediaSourceId` 查询字段；`master/main/live` 命名 manifest 会按请求文件名从受限输出目录读取，manifest 内相对子播放列表和可识别 segment 会按视频/音频入口分别重写到 `/emby/Videos/{Id}/...` 或 `/emby/Audio/{Id}/...`（主重写使用 `hls1` 官方形态，legacy `/hls/...` 入口同样可读）；客户端结束 HLS 后可通过 `DELETE /Videos/ActiveEncodings` 或 `/Audio/ActiveEncodings`（含 `/audio` 别名）按当前用户、`PlaySessionId`/`playSessionId` 和可选 `DeviceId`/`deviceId` 取消 queued / running 转码会话，并 best-effort 清理受限于 `TRANSCODE_CACHE_DIR` 的 session 输出目录；转码失败或状态已被取消/丢失时，worker 也会再次尝试清理输出目录并记录失败原因；`core.transcode.cleanup` 会按 `SCHEDULE_TRANSCODE_CLEANUP` 周期重试 failed / cancelled 且尚未标记清理完成的输出目录。
- 转码输出目录清理链路已补真实 schema 冒烟：`transcode_cleanup_sql_executes_against_live_schema` 会在迁移后的 PostgreSQL 上校验 `idx_transcoding_sessions_output_cleanup_pending` 定义、`TRANSCODE_CLEANUP_CANDIDATES_SQL` 的 `EXPLAIN` 计划、终态 session 候选读取、`mark_transcode_output_cleaned` 的 UUID-safe 更新和重复/非法 id no-op，避免后续调整迁移或 SQL 时让 `core.transcode.cleanup` 悄悄漂移。
- Emby VideoService 文件名流入口已补官方 `Videos/{Id}/{StreamFileName}` 和大小写/`/emby` 别名：无 `TranscodeSessionId` 时复用 direct-stream 鉴权、Range、本地文件/安全 STRM 边界，带 `TranscodeSessionId` 时继续按 HLS 输出文件读取分流；`Videos/{Id}/stream` 与 `Videos/{Id}/stream.{Container}` 的 `HEAD` 播放探测已纳入路由覆盖测试。
- Emby AudioService 官方直放入口已补 `Audio/{Id}/stream` 和 `Audio/{Id}/stream.{Container}`（`/emby` + plain + lowercase `/audio` 别名），复用既有 direct-stream 鉴权、`MediaSourceId`/`mediaSourceId`、Range、本地文件和安全 STRM 边界；同时补 `Videos/{Id}/AlternateSources` 删除和 `Videos/MergeVersions` 合并版本探测入口，按服务器管理员权限保护并规范化 `Id`/`Ids`，当前返回受控冲突，避免真实多版本模型未落地前 404 或假成功。
- Emby SubtitleService 管理探测已补 `Providers/Subtitles/Subtitles/{Id}`、`Items/{Id}/RemoteSearch/Subtitles/{SubtitleId}`、`Items/Videos/{Id}/Subtitles/{Index}` 删除别名和 `Videos/{Id}/{MediaSourceId}/Attachments/{Index}/Stream`；入口按认证用户和用户可见媒体项边界保护，远程字幕搜索/下载/删除、字幕流和 HLS 字幕播放列表查询均兼容 PascalCase 与 lower-camel 字段（如 `MediaSourceId`/`mediaSourceId`、`UserId`/`userId`、`StartPositionTicks`/`startPositionTicks`、`SubtitleSegmentLength`/`subtitleSegmentLength`），远程字幕下载、字幕删除和内嵌附件流当前返回受控冲突，避免真实 provider、索引和附件抽取模型未设计完成前制造未落库成功状态。
- Emby 音乐播放已显式补官方 `/Audio/{Id}/universal`、`/Audio/{Id}/universal.{Container}` 与 lowercase `/audio/{Id}/universal` / dotted 别名，普通请求复用现有流式读取、STRM allowlist 和用户可见权限边界，dotted route 的 `{Container}` 会作为 `Container=` query 的 fallback，`TranscodingProtocol=hls` 请求会创建 audio-only HLS 转码 session 并跳转到 `/emby/Audio/{Id}/master.m3u8`；Universal Audio 的 `UserId`/`userId` 查询参数在直连和 HLS 分支都会与当前认证用户校验，其他查询参数兼容 PascalCase / lower-camel 后再做 allowlist/范围规范化（含 `MediaSourceId`/`mediaSourceId`、容器、转码协议、`MaxStreamingBitrate`/`maxStreamingBitrate`、`AudioBitRate`/`audioBitRate`、采样率、播放 session 和起始 ticks），码率仍以前者优先、后者作为 fallback，并兼容 `DELETE /Audio/ActiveEncodings` 播放结束清理别名，完整渐进式音频转码语义后续继续补齐。
- Emby/Jellyfin 音乐歌词探测已补 `Audio/{Id}/Lyrics` 和 `Items/{Id}/Lyrics`，按当前用户可见权限读取音频同目录同名 `.lrc` / `.elrc` / `.txt` sidecar 并返回兼容 `LyricDto`，查询字段兼容 PascalCase、lower-camel 和 snake_case（`UserId`/`userId`/`user_id`）；`Audio/{Id}/RemoteSearch/Lyrics` 同样兼容 `MediaSourceId`/`mediaSourceId`、`ProviderName`/`providerName` 和 `SearchTerm`/`searchTerm`，返回受权限保护的空远程搜索结果，后续可继续补歌词扫描入库、缓存和插件 provider。
- Emby Items / 音乐列表 / 搜索提示高频查询字段已补 lower-camel/snake_case 兼容：`/Users/{Id}/Items`、`Albums`、`Songs`、`Artists/{Id}/Songs`、`Artists/{Id}/Albums`、`Search/Hints` 和 `Items/{Id}?UserId=...` 可解析 `parentId`、`startIndex`、`includeItemTypes`、`imageTypeLimit`、`searchTerm`、`genreIds`、`artistIds`、`albumIds`、`mediaTypes`、`audioCodecs` 等客户端常见字段，解析后仍走原有 allowlist/normalized filter 与 repository 权限过滤。
- Emby Items 详情/推荐辅助查询字段已补 lower-camel/snake_case 兼容：`Artists/InstantMix?Id=...`、`MusicGenres/InstantMix?Id=...`、`Items|Songs|Albums/{Id}/InstantMix`、`Videos/{Id}/AdditionalParts`、`Items/{Id}/CriticReviews`、`Items/{Id}/SpecialFeatures`、`Items/{Id}/Intros`、`Items/{Id}/LocalTrailers`、`Items/Counts`、`Users/{Id}/Suggestions`、`Movies/Recommendations`、`Trailers` 和 `Items|Movies|Albums|Artists/{Id}/Similar` 可解析 `userId`、`startIndex`、`itemLimit`、`categoryLimit`、`enableImages`、`imageTypeLimit`、`enableImageTypes`、`enableUserData`、`includeItemTypes`、`mediaTypes`、`searchTerm`、`sortBy`、`sortOrder` 等常见客户端字段，避免详情页、首页推荐和辅助探测因命名差异 422。
- Emby Shows / Prefixes 高频浏览字段已补 lower-camel/snake_case 兼容：`Shows/{Id}/Seasons`、`Shows/{Id}/Episodes`、`Shows/NextUp`、`Shows/Upcoming`、`Items/Prefixes` 和 `Artists/Prefixes` 可解析 `userId`、`seasonId`、`seriesId`、`parentId`、`startIndex`、`includeItemTypes`、`sortBy`、`sortOrder`、`fields`、`searchTerm`、`nameStartsWith`、`nameStartsWithOrGreater`、`nameLessThan` 和 `artistType` 等常见字段，继续复用原有分页、排序 allowlist 与 repository 权限过滤。
- Emby 启动/浏览辅助入口 query 字段已补 lower-camel/snake_case 兼容：`UserViews`、`Library/MediaFolders`、`Library/VirtualFolders/Query`、`Devices`、`Devices/Info`、`Devices/Options`、`Devices/Delete`、`Users/{Id}/Sections/{SectionId}/Items`、`Items/{Id}/ThemeMedia`、`ThemeSongs` 和 `ThemeVideos` 可解析 `userId`、`isHidden`、`sortOrder`、`id`、`inheritFromParent`、`parentId`、`startIndex`、`limit`、`includeItemTypes`、`mediaTypes`、`imageTypeLimit` 和 `fields` 等常见字段，避免启动同步、媒体库选择、首页分区、设备管理和主题媒体探测因命名差异 422。
- Emby Genres / Artists / Persons / Studios / classification 浏览查询字段已补 lower-camel/snake_case 兼容：`Genres`、`MusicGenres`、`Artists`、`Artists/AlbumArtists`、`Persons`、`Studios`、`Tags`、`OfficialRatings`、`Years`、`Containers`、`AudioCodecs`、`VideoCodecs`、`SubtitleCodecs`、`StreamLanguages` 和 name 详情入口可解析 `userId`、`parentId`、`startIndex`、`searchTerm`、`sortOrder`、`nameStartsWith`、`nameStartsWithOrGreater`、`enableImages`、`artistType`、`albumIds`、`personTypes` 等常见客户端字段。
- Emby Studios 浏览已补 `Studios` 和 `Studios/{Name}`，按当前用户可见媒体项聚合制作公司，支持分页、搜索、父级范围和基础名称排序。
- Emby Tags / OfficialRatings / Years / technical facets 浏览已补 `Tags`、`OfficialRatings`、`Years`、`Containers`、`AudioCodecs`、`VideoCodecs`、`SubtitleCodecs` 和 `StreamLanguages`，按当前用户可见媒体项聚合标签、官方分级、生产年份、容器、编解码器和流语言，支持分页、搜索、父级范围和基础排序；`Items/Filters` 已补 legacy 筛选面板入口，返回 `Genres`、`Tags`、`OfficialRatings` 和 `Years`；`Items/Filters2` 已补官方 `QueryFilters` 入口，复用同一权限过滤聚合返回 `Genres: NameLongIdPair[]`、空 `Studios` 和 `Tags`（真实 facet id 模型接入前 genre id 保持受控 0）；两者都兼容 `ParentId`/`parentId`、`IncludeItemTypes`/`includeItemTypes` 和 `MediaTypes`/`mediaTypes` 缩小聚合上下文。
- Emby 管理员用户列表和索引已补 `Users/Query`、`Users/ItemAccess` 与 `Users/Prefixes`，按管理员权限返回 `QueryResult<UserDto>` 或去重后的 `NameIdPair[]` 前缀，支持 `IsHidden`、`IsDisabled`、`StartIndex`、`Limit`、`NameStartsWithOrGreater` 和 `SortOrder` 查询字段的 PascalCase/lower-camel/snake_case 受控映射，并钳制病态超大 `StartIndex`。
- Emby 用户 ID 登录已补 `POST Users/{Id}/Authenticate`，按官方 `Pw` payload 复用真实密码校验、设备策略、session 生成和登录 hook，仓储层使用 `public_id = case ... then $1::uuid` 的索引友好查询形态。
- Emby 忘记密码探测已补 `Users/ForgotPassword` 和 `Users/ForgotPassword/Pin`，按官方 DTO 解析 `EnteredUsername` / `Pin` 并兼容 lower-camel/snake_case body 字段，返回 `ContactAdmin` 或失败的 PIN 兑换结果，不生成 PIN 文件、不落库修改密码，后续真实密码重置状态机设计完成后再接持久化流程。
- Emby 用户管理写入口已补 `POST Users/New`、`POST Users/{Id}`、`DELETE Users/{Id}`、`POST Users/{Id}/Delete`、`POST Users/{Id}/Configuration`、`POST Users/{Id}/Configuration/Partial`、`POST Users/{Id}/Policy`、`POST Users/{Id}/Password` 和 `POST Users/{Id}/EasyPassword`；入口按本人或管理员边界保护，受控解析创建用户、密码与 easy password DTO、通用用户/configuration/policy JSON/XML body、规范化路径和字段、body 字段兼容 lower-camel/snake_case、限制 64 KiB payload，畸形 payload 返回 422，并返回受控冲突，避免真实用户生命周期、策略和密码模型外制造未落库成功状态。
- Emby ConnectService 探测已补 `Connect/Exchange`、`Connect/Pending`、`Users/{Id}/Connect/Link`、`DELETE Users/{Id}/Connect/Link` 和 `Users/{Id}/Connect/Link/Delete`；入口按认证用户或本人/管理员边界保护，受控解析 Connect username、PIN 和 link body，读探测返回未启用状态，link/unlink 返回受控冲突，避免外部 Emby Connect 账号绑定、兑换和审计模型未设计完成前客户端探测 404 或制造假成功。
- Emby 用户详情顶层 DTO 已显式补齐常见客户端字段：`ServerName`、Connect 账号/link 类型、头像 tag/比例、最近登录/活跃时间、创建时间、分享级别和前缀返回稳定默认或 `null`，后续真实用户资料/头像/审计模型落地后再接持久化。
- Emby 用户详情 `Policy` 已显式补齐常见客户端策略字段：播放转码布尔会同步到 `ForceRemoteSourceTranscoding`，频道 allow/block、blocked media folders 和 allowed/blocked tags 在真实模型落地前返回空数组，远程控制其他用户、Live TV 管理/访问、共享设备控制和最大活跃会话字段保持保守默认，避免 `Users/Me` / `Users/{Id}` 策略探测因缺失字段产生未知能力分支。
- Emby 用户详情 `Policy` 已显式补齐访问限制和家长控制默认字段：隐藏状态、锁定时间、最大分级、标签 allow/block 模式、访问时间表、未分级屏蔽、restricted features、认证 provider、子目录排除、相机上传和个人共享均返回当前未启用能力的空/`null`/`false` 默认值，后续真实家长控制/访问计划模型落地后再接持久化。
- Emby 用户详情 `Configuration` 已显式补齐常见客户端播放偏好字段：`HidePlayedInMoreLikeThis`、`HidePlayedInSuggestions`、`IntroSkipMode`、`ProfilePin` 和 `ResumeRewindSeconds` 返回保守默认值，后续真实用户偏好持久化模型落地后再接写入/读取。
- Emby API key 管理探测已补 `Auth/Keys`、`DELETE Auth/Keys/{Key}` 和 `Auth/Keys/{Key}/Delete`，按管理员权限保护并受控解析分页、`App` 和 key 路径，钳制病态超大 `StartIndex`；当前 `GET` 返回空 `QueryResult`，删除为兼容 no-op，创建长期 key 暂不启用，后续需先设计真实密钥生命周期和审计。
- Emby 显示偏好写入口已补 `POST DisplayPreferences/{Id}`，按认证用户和 `UserId`/`userId` 边界接收并规范化 `SortBy`/`sortBy`、`SortOrder`/`sortOrder`、`Client`/`client` 和 `CustomPrefs`/`customPrefs`，当前作为兼容写入边界返回官方空成功响应，后续可接真实偏好持久化。
- Emby 用户设置入口已补 `UserSettings/{UserId}`、`POST UserSettings/{UserId}`、`POST UserSettings/{UserId}/Partial`、`Users/{Id}/TypedSettings/{Key}`、`POST Users/{Id}/TypedSettings/{Key}`、`DELETE Users/{Id}/TrackSelections/{TrackType}` 和 `POST Users/{Id}/TrackSelections/{TrackType}/Delete`，按认证用户边界返回兼容空设置字典或接收受尺寸上限保护的 key/value、typed setting、binary payload，`UserSettings` 写入 body 兼容 `Name`/`name`、`Key`/`key` 和 `Value`/`value`，并对 Audio/Subtitle track selection 清理返回 no-op 成功；后续可接真实用户偏好持久化。
- Emby 会话详情已补 `Sessions/{Id}`，按当前用户返回单个活跃 session，仓储层同时约束 session 归属、过期、撤销和设备撤销状态。
- Emby 会话列表 `Sessions` 已补 `UserId` / `userId` / `user_id` 和 `DeviceId` / `deviceId` / `device_id` 查询字段兼容，继续按当前认证用户边界返回活跃会话，并把可选设备过滤下推到仓储 SQL，避免客户端启动会话同步或播放远控 session 定位因字段命名差异 422。
- Emby 会话结束已补官方 `Sessions/Logout` 空响应形态，使用当前 access token 撤销会话，避免客户端登出阶段收到非官方 JSON 响应。
- Emby session capabilities 上报已补 lower-camel 字段兼容：`Sessions/Capabilities` query 和 `Sessions/Capabilities/Full` body 同时接受 `PlayableMediaTypes`/`playableMediaTypes`、`SupportedCommands`/`supportedCommands`、`SupportsMediaControl`/`supportsMediaControl`、push token、device profile 等字段，避免真实客户端能力上报因命名差异落空或 422。
- Emby 播放前网络测速已补 `Playback/BitrateTest?Size=...`，按认证用户返回受 64 MiB 上限保护的流式二进制响应，避免自动码率探测阶段 404 或无界内存分配。
- Emby PlaybackInfo POST 已补 query-only 客户端形态：`Items/{Id}/PlaybackInfo` 和 `Users/{Id}/Items/{Id}/PlaybackInfo` 的 POST 在 body 为空时可直接从 query string 解析 `UserId`/`userId`、`MediaSourceId`/`mediaSourceId`、`MaxStreamingBitrate`/`maxStreamingBitrate`、`StartTimeTicks`/`startTimeTicks` 和 `DeviceProfile`/`deviceProfile`；有 JSON/XML body 时 body 字段优先、query 字段只补缺省，避免真实客户端把 PlaybackInfo 参数放在 URL 上或用 lower-camel body/query 字段时被 `request body is required` / 字段落空拒绝。
- Emby PlaybackInfo 转码 URL 已按媒体类型分流：视频媒体源继续返回 `/emby/Videos/{Id}/master.m3u8`，音乐 track/audio 媒体源返回 `/emby/Audio/{Id}/master.m3u8`，与已注册的音频 HLS manifest / segment / `/Audio/ActiveEncodings` 路由对齐。
- Emby 本地 direct stream / download 单段 Range 已补 suffix byte range 和越界语义：`bytes=10-` 和 `bytes=-500` 均能规范化为受控 `206 Partial Content` 范围，超出资源长度的 range 返回 `416 Range Not Satisfiable`，多段 range 仍拒绝，避免播放器或下载器请求尾部探测时被 `invalid range start` 拒绝。
- Emby BifService 已补 `Videos/{Id}/index.bif` 和 `Items/{Id}/ThumbnailSet`；入口按认证用户和媒体库可见权限保护，`Width` 参数先做必填、正数和上限校验，`ThumbnailSet` 在尚未生成缩略图时返回空 `ThumbnailSetInfo`，`index.bif` 返回受控 not found，避免被通用视频文件名路由误吞或制造假 BIF 成功状态。
- Emby live stream 媒体信息入口已补 `LiveStreams/Open`、`LiveStreams/MediaInfo` 和 `LiveStreams/Close` 的认证保护兼容空边界，并兼容 lower-camel 的 `openToken`、`userId`、`playSessionId` 和 `liveStreamId` 字段，避免未接真实 live source 状态机前客户端播放媒体信息探测 404/422。
- Emby 全局播放上报入口 `Sessions/Playing`、`Sessions/Playing/Progress` 和 `Sessions/Playing/Stopped` 已补 query-only 客户端形态：body 为空时可从 query string 解析 `ItemId`/`itemId`、`UserId`/`userId`、`PlaySessionId`/`playSessionId`、`MediaSourceId`/`mediaSourceId`、`PlayMethod`/`playMethod`、`PositionTicks`/`positionTicks`、暂停状态和既有扩展字段；有 body 时继续保留原 `PlaybackProgressDto` 解析（含 `Item`/`item` 对象补 `ItemId`、JSON/XML body），query 仅补缺省字段，避免真实客户端播放状态同步在空 body + query 参数形态下 422。
- Emby 播放 session 保活已补 `Sessions/Playing/Ping`，按认证用户接收并规范化可选 `PlaySessionId`/`playSessionId`，避免客户端播放保活阶段 404/422，后续可接真实 session state 心跳更新时间。
- Emby 老式播放上报入口已补 `Users/{Id}/PlayingItems/{ItemId}`、`Users/{Id}/PlayingItems/{ItemId}/Progress`、`DELETE Users/{Id}/PlayingItems/{ItemId}` 和 `Users/{Id}/PlayingItems/{ItemId}/Delete`，复用现有播放开始/进度/停止写入与 hook 派发逻辑，并支持路径参数加 query-only 或 JSON body 的客户端上报形态，同时保留常见播放状态扩展字段。
- Emby 播放上报 DTO 已兼容 `Item`/`item` 对象补 `ItemId`，并解析 PascalCase / lower-camel 的 `QueueableMediaTypes`、`CanSeek`、`EventName`、音轨/字幕轨、静音、音量、直播流、播放队列位置、当前播放队列、重复/随机/睡眠定时和播放速率等常见客户端状态字段；`QueueableMediaTypes` / `PlaylistItemIds` 列表字段同时接受 JSON 数组、query/form 序列和逗号分隔字符串，后续可接真实 session state。
- Emby Sessions 远程控制入口已补 `/Sessions/{Id}/Playing`、`Playing/{Command}`、带 session id 或不带 session id 的 `Command/{Command}`、`System/{Command}`、`Message` 和 `Viewing` 的兼容 no-op 边界；远程播放命令 query/body 字段兼容 PascalCase 与 lower-camel（`ItemIds`/`itemIds`、`PlayCommand`/`playCommand`、`StartPositionTicks`/`startPositionTicks`、`SeekPositionTicks`/`seekPositionTicks` 等），通用命令、消息和 viewing body 字段兼容 PascalCase/lower-camel/snake_case，`Message` / `Viewing` 也支持 body 为空时从 query string 补齐轻量字段，避免真实客户端命令 payload 因字段命名差异或 query-only 形态被解析为空或 422。
- Emby session additional-user 入口已补 `Sessions/{Id}/Users/{UserId}`、`DELETE Sessions/{Id}/Users/{UserId}` 和 `Sessions/{Id}/Users/{UserId}/Delete` 的认证保护兼容空响应边界，后续可接真实多人 session state。
- Emby 原始文件入口已补官方 `Items/{Id}/File`，复用 `Items/{Id}/Download` 的用户/媒体库下载权限、`MediaSourceId`/`mediaSourceId`、Range、本地文件/安全 STRM 302 和下载 hook 边界，避免原始文件读取绕过下载权限。
- Emby 计划任务管理探测 `ScheduledTasks`、`ScheduledTasks/{Id}` 和 `ScheduledTasks/Running/{Id}` 已补管理员保护的兼容边界，列表查询 `IsHidden` / `IsEnabled` 字段兼容 lower-camel/snake_case，手动运行/停止继续复用现有 scheduler service。
- `/ready` 已真实探测 PostgreSQL `select 1` 和 Redis `PING`，并支持 `FBZ_READINESS_TIMEOUT_MS`。
- `/ready` 已返回 worker/scheduler 配置、节点角色运行条件、全局队列 backlog、过期租约、调度器运行状态和当前节点负责消化/接管的摘要，便于 Docker/NAS 多节点部署排障。
- `/ready` 已补 `runtime.roles`，按 `FBZ_NODE_ROLE=all/api/worker/scheduler` 明确暴露当前进程承担的 api、worker、scheduler 职责，便于 API-only、worker-only 和 scheduler-only 节点排障。
- `/ready` 已补 `runtime.scheduler`，汇总 due tasks、active running runs、expired running runs 和 manual running runs，并用 `drained_by_node` 标明当前节点是否真的承担 scheduler 职责；该字段是调度器观测信息，不会因为存在 due/running/expired run 而单独把 readiness 判为 unhealthy。
- `/ready` 已补 `runtime.queues.event_stream_mirror`，按 PostgreSQL `event_outbox` 未镜像事件统计 unmirrored、claimable、locked、backoff、failed、max_attempts 和 expired_leases，便于定位 Redis Streams 镜像 worker 停滞、回退或旧锁待接管。
- `/ready` 队列摘要已补按节点职责的 `drained_by_node` 标记：jobs / event_outbox / transcodes / notifications / event_stream_mirror 各队列的 backlog 计数是全局的，新增布尔标记表明当前节点是否真的运行消费该队列的 worker（均需 worker 角色，且各自对应 worker 启用）。映射按真实消费者：generic `jobs` 由 scan/metadata/probe worker 消费；transcodes 由转码 worker；notifications（`plugin_notification_requests`）由通知投递 worker；`event_outbox` 由三类消费者共享——插件 hook 派发（plugins::execution）、通知投递（notifications::delivery）和 Redis Streams 镜像（events），任一在 worker 节点启用即视为本节点消费；`event_stream_mirror`（`stream_mirrored_at is null` backlog）专属 Redis 镜像 worker。api-only / scheduler-only 节点能看到全局 backlog 但 `drained_by_node=false`，便于多节点排障时区分「本节点负责的积压」与「可见但不归本节点处理的积压」。
- `/ready` 队列摘要顶层已补 `assigned_backlog` 和 `has_assigned_backlog`，只汇总 `drained_by_node=true` 的 jobs / event_outbox / transcodes / notifications backlog，以及本节点负责 Redis Streams 镜像时的 unmirrored 事件数；该字段是观测信息，不会因为存在积压而把 readiness 判为 unhealthy。
- `/ready` 队列摘要已补各队列 `expired_leases` 以及顶层 `assigned_expired_leases` / `has_assigned_expired_leases`，用于区分全局过期租约和当前节点负责接管/回收的过期租约；event outbox 主 dispatch/delivery 租约只归插件或通知 worker，Redis Streams 镜像租约只归 event stream mirror worker；`plugin_notification_requests` 自身无租约列，通知投递过期租约通过 `event_outbox.expired_leases` 观测。该字段同样只做观测，不因过期租约存在而把 readiness 判为 unhealthy。
- HTTP 请求追踪已补 `HTTP_SLOW_LOG_THRESHOLD_MS` 慢请求阈值，超过阈值的请求会写入包含 status、latency_ms 和 threshold_ms 的 `slow http request` 结构化 warn 日志。
- SQLx 慢 SQL 观测已补 `DATABASE_SLOW_LOG_THRESHOLD_MS`，数据库连接会按配置把超过阈值的 SQL 语句写入 warn 日志，同时保留 PostgreSQL `statement_timeout` 作为硬超时。
- 通用 job lease 回收已补 `recovered stale running jobs` 结构化 warn 日志，按 job_type 记录 expired_jobs、retryable_jobs 和 terminal_jobs，便于区分可重试过期任务与达到 max attempts 的终止任务。
- 通用 job handler 失败重试路径已补结构化 warn 日志：`library.scan`、`metadata.refresh`、`media.probe` 三个 worker 的失败收尾统一走 `jobs::mark_job_failed`，释放租约后按 `attempts < max_attempts` 输出 `job failed; scheduled retry`（retryable=true）或 `job failed; max attempts reached`（retryable=false），记录 job_type、job public_id、attempt/max_attempts 和 error；此前 handler 直接报错（如扫描期间 NAS 临时不可达）只把 job 标记 failed 而无日志，仅 lease 过期路径可观测。
- 计划任务 run lease 回收已补 `recovered stale scheduled task runs` 结构化 warn 日志，记录 expired_runs、due_runs 和 manual_runs，便于区分周期调度和管理员手动触发的过期任务。
- 计划任务 run 执行失败路径已补 `scheduled task run failed` 结构化 warn 日志：`run_next_due_task`（周期调度）和 `run_task_once`（管理员手动触发）在 `mark_task_failure` 落库后统一调用 `log_scheduled_task_failure`，记录 task_key、task_type、run_id 和 error；此前执行期失败只更新 `scheduled_task_runs`/`scheduled_tasks`，仅由 worker 循环输出不带任务身份的通用 `scheduler worker failed to dispatch task`，手动触发路径无日志。
- 转码 lease 回收已补 `recovered stale transcode sessions` 结构化 warn 日志，记录 expired_sessions、retryable_sessions 和 terminal_sessions，便于区分可重新排队的过期转码与达到 max attempts 的终止转码。
- 转码 worker 失败路径已补 `transcode session failed` 结构化 warn 日志上下文，记录 session、user、item、media_file_id、worker、attempts/max_attempts、硬件加速、编码容器和 bitrate，便于区分硬件/编码/特定媒体项导致的失败。
- 插件 execution run lease 回收已补 `recovered stale plugin execution runs` 结构化 warn 日志，记录 expired_runs 和 revoked_tokens，便于定位插件 worker 崩溃后执行审计与 Host Token 回收。
- Redis Streams 事件镜像 lease 接管已补 `recovered stale event stream mirror leases` 结构化 warn 日志，记录 stream key、worker id、recovered_stale_leases 和 claimed，便于定位镜像 worker 崩溃后旧锁接管。
- Redis Streams 事件镜像发布失败并进入回退等待时，已补 `event stream mirror publish failed; scheduled retry` 结构化 warn 日志，记录事件 public id、event type、stream key、mirror attempts 和 retry delay，便于定位队列重试风暴。
- 插件 hook dispatch 失败并重新入队时，已补 `plugin dispatch failed; scheduled retry` 结构化 warn 日志，记录 outbox public id、attempt/max_attempts 和 retry delay，便于定位插件执行失败重试。
- 插件 hook dispatch worker 过期 outbox lease 接管已补 `recovered stale plugin dispatch lease` 结构化 warn 日志：claim SQL 改为 `with claimed as (...) update ... from claimed`，在同一原子语句内捕获领取前 `status`/`locked_by`，`prior_status == 'delivering'` 时记录 outbox public id、`prior_locked_by`、attempt/max_attempts，避免插件 worker 崩溃后旧锁接管与普通 pending/failed 领取混在一起。新增源码结构守卫 `plugin_dispatch_stale_lease_reclaim_logs_prior_state`，并新增 live DB 非破坏性 `EXPLAIN` 冒烟 `plugin_dispatch_claim_sql_plans_against_live_schema`，同时校验 `0044` 两个插件 dispatch claim partial index 在真实迁移 schema 上存在。
- 通知投递 outbox 失败并重新入队时，已补 `notification delivery failed; scheduled retry` 结构化 warn 日志，记录 outbox public id、attempt/max_attempts 和 retry delay，便于定位通知投递重试。
- 通知投递 worker 过期租约接管已补 `recovered stale notification delivery lease` 结构化 warn 日志：此前 `claim_next_event` 通过 `status='delivering' and locked_until <= now()` 原子重领崩溃 worker 遗留的在途投递，但与领取新 `pending`/`failed` 事件无法区分、无可观测。现在 claim 查询用 CTE 在同一原子语句内捕获前序 `status`/`locked_by`，`prior_status == 'delivering'` 即判定为过期租约接管并写入结构化 warn，记录 outbox public id、`prior_locked_by`、attempt 和 max_attempts，与其他四个 worker 的 `recovered stale …` 日志对齐。claim SQL 抽成 `CLAIM_NEXT_EVENT_SQL` const（生产方法与实跑冒烟共用，避免重复）。补 `stale_lease_reclaim_captures_prior_state_and_logs_structured_warn` 源结构守卫测试，并新增 `#[ignore]` 实跑冒烟 `claim_next_event_executes_against_live_schema`。本地 dockerized PostgreSQL **已实跑通过**：用 `EXPLAIN`（非 ANALYZE）对生产 SQL 做 parse/plan/类型校验，完整解析 `UPDATE...FROM claimed` CTE、全部列/类型与前序状态 RETURNING，**不执行 UPDATE、不改动任何真实排队事件**（首版实跑用真实 claim 时发现 dev DB 已有可领取通知事件、会真改库，故改为非破坏性 EXPLAIN）。`cargo test -- --ignored claim_next_event_executes_against_live_schema` 绿。
- 通知投递 worker 的 event outbox claim 热路径索引已对齐插件 dispatch：新增迁移 `0075_notification_delivery_claim_indexes.sql`，为 `event_type='notification.send.requested'` 分别补 `(available_at, id)` pending/failed 可领取 partial index 和 `(locked_until, id)` delivering 过期租约 partial index，避免通知 worker 在高增长 `event_outbox` 上依赖泛化 `status/available_at` 索引扫描其他事件类型。新增源码结构守卫 `notification_delivery_claim_indexes_match_claim_query`，并扩展 live DB 冒烟 `claim_next_event_executes_against_live_schema` 校验迁移后的索引定义和生产 claim SQL 非破坏性 `EXPLAIN`。
- 数据库性能方向：多处列表和审计接口已改 keyset pagination；插件 Host API 媒体库 item 列表已移除 `startIndex` 触发的 offset/full-count 查询路径，统一使用 `(sort_key, id)` cursor keyset，并保持 `totalRecordCountIsExact=false` lower-bound 语义；Emby 播放列表列表和播放列表条目查询已移除 `count(*) over()` 全量窗口计数，改为 `limit + 1` lower-bound 计数以保留 `StartIndex` 响应窗口同时避免大播放列表全扫，并在兼容层钳制病态超大 `StartIndex`，避免深 offset 被客户端无限放大；Emby Items 族入口（普通 browse、latest/resume alias、similar、search hints、音乐详情列表、instant mix、trailers、special features、intros、critic reviews）同样在兼容层钳制病态超大 `StartIndex`，避免 offset 查询、内存 skip 或空结果回显被客户端放大；其中 `Items/Latest` / `Users/{Id}/Items/Latest` 数组入口也改为 `limit + 1` 哨兵读取后裁掉额外行，继续不暴露精确总数；Emby genres、artists、persons、studios、tags、official ratings、years、containers、codecs 和 stream languages 等分类/字典/人物聚合查询也已移除 exact `count(*) over()`，改用 `limit + 1` lower-bound 计数保留客户端分页响应；`Items/Prefixes` / `Artists/Prefixes` 首字母数组入口同样改为 `limit + 1` 哨兵读取并在 repository helper 中裁掉额外行，保持数组 DTO 不变且避免后续接深分页时退回精确计数；这些入口都在兼容层钳制病态超大 `StartIndex`，限制深 offset 风险；`Shows/{Id}/Seasons` 和 `Shows/{Id}/Episodes` 也已切到 `limit + 1` lower-bound 计数，避免大剧集子列表 exact count；普通 `/Users/{Id}/Items` browse、positive playstate fast path、`Ids` fast path 和 provider id fast path 已移除剩余 exact `count(*) over()`，统一通过 `limit + 1` 和 `browse_result_lower_bound_from_rows` 返回 lower-bound `TotalRecordCount`，避免大库浏览或窄索引 fast path 为精确总数全扫；Emby 管理员兼容 `Users/Query` / `Users/Prefixes` 已移除单独精确 `count(*)` 预查询，改用 `limit + 1` 探针和 lower-bound `TotalRecordCount` 保留 `StartIndex` 响应窗口，并钳制病态超大 `StartIndex`；`/ready` 队列摘要已从高频精确 `count(*) filter` 改为每个状态 `limit 10001` 的 bounded lower-bound 探针摘要，并新增迁移 `0070_readiness_event_outbox_indexes.sql` 覆盖 event outbox status/lease 与 mirror failed 采样形态，避免 readiness 在高增长队列表上扫全表；Admin Redis Streams 镜像状态 `/api/admin/event-stream-mirror/status` 也已从全量未镜像 outbox 聚合改为同样的 bounded lower-bound 摘要，响应新增 `countsAreExact` / `sampleLimit` 精度元数据，并新增迁移 `0071_admin_event_stream_mirror_status_indexes.sql` 支撑 oldest-unmirrored 状态查询；Host API 调用审计已补单条件和 `pluginId/statusCode`、`executionRunId/statusCode` 组合过滤索引，并补真实 schema 冒烟覆盖 Admin Host API 审计的 plugin/status、run/status、cursor、token/run join 和非法 run id no-op；Admin job 列表和 job run/event 历史查询已补真实 schema 冒烟覆盖 job 列表 status/type/queue/cursor、分区后 `job_runs` / `job_events` 的 detail、status/level 和 cursor 查询。
- 数据库性能方向补充：活动日志、API key、远程图片、虚拟文件夹 Query、在线频道、Live TV 空列表和 Sync 列表等当前返回空或小结果的兼容边界也已在 route 层钳制病态超大 `StartIndex`，避免未来接入真实 repository 后继承无界深 offset / skip 风险。
- `/ready` scheduler 摘要已从精确统计全部 due tasks / running task runs 改为 bounded lower-bound 探针：due task 候选最多采样 `10001` 条，每个候选只按该 task 的 `max_concurrency` 有界探测 active running runs；running / expired / manual running run 也各自最多采样 `10001` 条，保留原 `due_tasks`、`running_runs`、`expired_runs`、`manual_running_runs` 字段但不再为高增长 `scheduled_task_runs` 做全表精确统计。新增迁移 `0072_readiness_scheduled_task_run_indexes.sql` 支撑全局 running lease 与 manual running lease readiness 采样，并新增源码结构守卫和 live DB 只读冒烟 `readiness_scheduler_summary_executes_against_live_schema`。
- Scheduler due/manual claim 并发门控已从精确统计某 task 的全部 active running runs 改为按 `max_concurrency` 有界探测：due claim 的候选任务 SQL 在仓储内用 `limit st.max_concurrency` 判定是否仍可领取，管理员/Emby 手动触发路径复用 `ACTIVE_SCHEDULED_TASK_RUN_CAPACITY_PROBE_SQL` 并绑定该任务的 `max_concurrency`；两条路径均保留原并发上限语义，同时避免异常大量 running run 历史分区时每次 claim 扫完全部 active runs。新增源码结构守卫 `scheduled_task_claim_concurrency_uses_bounded_probes` 和 live DB 只读冒烟 `scheduled_task_claim_capacity_probes_execute_against_live_schema`。
- Admin scheduled task 列表/详情的 `active_run_count` 摘要已从每个 task 精确统计全部 active running runs 改为复用 `SCHEDULED_TASK_ACTIVE_RUN_COUNT_SQL` 的 `limit tasks.max_concurrency` 有界探针；该字段继续用于管理页/Emby 计划任务 DTO 的 Running/Idle 与当前并发摘要，精确运行历史仍由 scheduled-task run keyset 列表承担。新增源码结构守卫 `scheduled_task_admin_active_run_counts_are_bounded` 和 live DB 实跑冒烟 `scheduled_task_admin_active_run_counts_execute_against_live_schema`；scheduled-task run 历史页本身也已补真实 schema 冒烟 `scheduled_task_run_history_queries_execute_against_live_schema`，覆盖分区后 `scheduled_task_runs` 的 status/cursor、非法 cursor 和缺失 task key no-op。
- Admin user 列表和 policy 更新返回的 `device_count` / `active_session_count` 摘要已从每个用户精确统计全部设备/active session 改为 `ADMIN_USER_SUMMARY_SAMPLE_LIMIT=10000` 的 bounded probe：设备按 `devices(user_id, last_seen_at desc)` 采样，active session 按 `sessions(user_id, revoked_at, expires_at)` 谓词采样；字段 shape 保持不变，语义变为管理页 lower-bound 摘要，精确设备/会话审计后续应走专门 keyset 明细。新增源码结构守卫 `admin_user_summaries_use_bounded_probes` 和 live DB 实跑冒烟 `admin_user_summary_probes_execute_against_live_schema`。
- Emby `Items/Counts` / `Users/{Id}/Items/Counts` 已从对当前用户可见 `media_items` 的精确全量 `count(*) filter` 聚合改为各类型与总数各自 `limit 10001` 的 bounded lower-bound 探针摘要；仓储层仍做 `library_permissions.can_view` + 非隐藏库权限过滤，字段 shape 保持 Emby 兼容，避免首页/媒体库概览在百万级可见媒体上全扫。新增源码结构守卫 `item_counts_query_uses_bounded_lower_bound_samples` 和 live DB 实跑冒烟 `item_counts_query_executes_against_live_schema`。
- 转码 worker claim 并发门控已从精确统计全部 active running sessions 改为按 `max_concurrent` 有界探测，保留默认 3 并发排队语义，同时避免异常大量 running 转码行时全量计数；新增源码结构守卫 `claim_next_uses_bounded_capacity_probe` 和 live DB 只读冒烟 `transcode_claim_capacity_probe_executes_against_live_schema`。
- Scheduler stale run lease 回收已从一次性更新全部过期 `scheduled_task_runs` 改为按 `(lease_expires_at asc, id asc)` 选取最多 1000 个候选并 `FOR UPDATE SKIP LOCKED` 后有界标记 `expired`，继续汇总 due/manual 触发来源并复用 `0072` 的全局 running lease 索引，避免调度器长时间停机后恢复形成大事务或多 scheduler 节点互相阻塞；新增源码结构守卫 `stale_scheduled_task_run_recovery_uses_bounded_locked_candidate_batch` / `stale_scheduled_task_run_recovery_index_matches_candidate_batch_shape` 和 live DB 非破坏性 `EXPLAIN` 冒烟 `stale_scheduled_task_run_recovery_sql_plans_against_live_schema`。
- 转码 stale lease 回收已从一次性重排或失败全部过期 running `transcoding_sessions` 改为按 `(lease_expires_at asc, id asc)` 选取最多 1000 个候选并 `FOR UPDATE SKIP LOCKED` 后有界更新，继续按 `attempts/max_attempts` 区分 retryable/terminal 汇总日志，并复用 `0013` 的 `idx_transcoding_sessions_running_lease`，避免转码 worker 长时间停机后恢复形成大事务或多 worker 互相阻塞；新增源码结构守卫 `stale_transcode_lease_recovery_uses_bounded_locked_candidate_batch` / `stale_transcode_lease_recovery_index_matches_candidate_batch_shape` 和 live DB 非破坏性 `EXPLAIN` 冒烟 `stale_transcode_lease_recovery_sql_plans_against_live_schema`。
- 插件 execution stale run 回收已从一次性失败全部匹配 running `plugin_execution_runs` 改为按 `(started_at asc, id asc)` 选取最多 1000 个候选并 `FOR UPDATE OF run SKIP LOCKED` 后再 join `event_outbox` lease/终态判断，继续撤销对应未撤销 Host Token，避免插件 worker 长时间停机后恢复形成大事务或多 worker 互相阻塞；新增迁移 `0074_plugin_execution_stale_recovery_index.sql`（`idx_plugin_execution_runs_stale_recovery` on `(started_at asc, id asc)` where running and unfinished）、源码结构守卫和 live DB 非破坏性 `EXPLAIN` 冒烟 `stale_plugin_execution_recovery_sql_plans_against_live_schema`。
- 通用 job stale lease 回收已从一次性更新某 `job_type` 的全部过期 running jobs 改为按 `(locked_until asc, id asc)` 选取最多 1000 个候选并 `FOR UPDATE SKIP LOCKED` 后有界更新，继续同步标记对应 `job_runs` 并保留 retryable/terminal 汇总日志，避免长时间停机后恢复形成大事务或多 worker 互相阻塞；新增迁移 `0073_stale_job_recovery_index.sql`（`idx_jobs_stale_recovery` on `(job_type, locked_until asc, id asc)` where running）支撑候选批次，新增源码结构守卫 `stale_job_recovery_uses_bounded_locked_candidate_batch` / `stale_job_recovery_index_matches_candidate_batch_shape` 和 live DB 非破坏性 `EXPLAIN` 冒烟 `stale_job_recovery_sql_plans_against_live_schema`。
- job 队列 claim 热路径索引已对齐：`metadata.refresh`（0015）和 `media.probe`（0023）此前各有专用 `(status, run_at, priority desc, id)` 部分 claim 索引，而 `library.scan` 只有按 libraryId 的去重索引（0021），claim 查询回退到跨 job_type 的通用 `idx_jobs_status_run_at`；新增迁移 0063 `idx_jobs_library_scan_claim` 补齐同形态部分索引，让每轮 worker 轮询的扫描 claim 在大表上保持选择性。已在本地 dockerized PostgreSQL 上实跑校验：迁移链 0001–0063 全部 `success=t`，`idx_jobs_library_scan_claim` 以预期定义创建（`btree(status, run_at, priority desc, id) where job_type='library.scan' and status in ('queued','failed')`），`enable_seqscan=off` 下规划器对扫描 claim 查询使用该索引（小表默认 seq scan 属正常）。
- `library.scan` 入队去重已对齐 worker 重试语义：scheduler 全库扫描和管理员手动扫描都会把 `queued` / `running` / retryable `failed`（`attempts < max_attempts`）视为同一媒体库的活跃任务，避免 NAS 临时失败后手动或定时扫描重复插入新 job；管理员手动入队会用 uuid-safe library lookup 读取 canonical public id，命中活跃任务时返回现有 job。新增源码结构守卫 `job_payload_dedupe_indexes_match_queue_queries` / `admin_queue_public_id_inputs_use_uuid_comparisons`，以及 live DB 实跑冒烟 `scan_queueing_reuses_retryable_failed_jobs_against_live_schema`。
- 本地开发依赖恢复：`scripts/dev-deps.ps1` 可一键 start/status/restart/stop PostgreSQL 和 Redis，并等待 Docker health 到位。
- 插件系统：manifest、权限、hook、计划任务、菜单、安装审批、HTTP/WASI runtime、Host API、通知 worker、运行审计和 Host API 调用审计。
- 插件开发生态：`docs/plugin-development.md`、HTTP helper、notification bridge 示例、Telegram / 企业微信 / webhook 通知模板、marker importer 示例、打包脚本、`sign-plugin-package` 签名工具、signed package smoke wiring、Host API budget runtime smoke、plugin notification delivery smoke、plugin schedule lifecycle smoke、plugin schedule dispatch smoke 和 Node helper / 模板结构测试。
- 元数据方向：TMDB / TVDB / Fanart provider 边界、镜像 URL 和代理配置。
- 元数据刮削子系统已从「硬编码 provider 链 + 只读环境变量」升级为可配置、可扩展架构（设计见 `docs/plans/metadata-scraper-design.md`）：阶段 0 把单文件 `provider.rs` 拆为 `provider/{mod,shared,tmdb,tvdb,fanart,imdb,plugin,proxy}.rs`，引入 `MetadataProvider` trait + `MetadataProviderRegistry` 取代硬编码 `match`；阶段 1 新增 additive 迁移 0077（`metadata_global_settings` 单行全局默认 + `metadata_provider_settings` per-provider 覆盖 + `metadata_provider_secrets` 加密 key 表），`metadata/settings.rs` 的 `MetadataSettingsRepository` + 纯函数 `resolve_metadata_config`（env 基线 ← DB 覆盖）+ 6 个 `/api/admin/metadata/*` 接口（GET/POST settings、POST provider、POST/DELETE key、POST test），key 复用 `SecretCipher`（scope=`metadata-provider`）加密、永不回显明文（仅 `hasKey`+末四位掩码），受 `every_admin_route_handler_enforces_server_admin` 守卫；阶段 2 修复此前被解析却未实现的 `no_proxy`/`policy`，实现 per-provider `inherit`/`direct`/`custom` 代理 client 工厂（按 (mode,url,no_proxy) 去重缓存）；阶段 3 实现图片语言独立于文本语言（`image_language`/`image_prefer_original`/`image_fallback_languages` + TMDB `/images?include_image_language=` + rank 排序选海报）和全局默认层折叠（库级←全局←provider 内置）；阶段 4 补 IMDb enrichment-only provider（规范 `tt` 前缀外部 ID）和 TVDB v4 extended 详情富化（genres/companies/characters→people best-effort）；阶段 5 基础层落地开放插件 provider 契约：新 hook 事件 `metadata.provider.query` + additive 迁移 0078 重定义 `plugin_hooks_event_key_allowed` 约束 + 激活 `metadata.read` 权限 + `PluginMetadataProvider` 适配器 + `PluginMetadataQuerier` 同步 seam（默认禁用）+ 纯函数 `merge_plugin_metadata`（内置优先、仅补空字段、external_id 去重、artwork scoped 追加），真正同步执行路径按设计标注待续。`cargo test --lib` 1068 全绿。
- 元数据刮削迁移与配置仓储已补真实 schema 冒烟：`metadata_settings_repository_executes_against_live_schema`（settings.rs）在迁移后的 dockerized PostgreSQL 上跑 0077 三表 DDL + `MetadataSettingsRepository` 全量往返（global upsert/load、provider upsert/load、secret set/加密 resolve 解密/providers_with_key/delete、`resolve_metadata_config` 折叠），用 `plugin:metadata-smoke` provider id 隔离并自清理（含恢复单行 global），关闭此前设计文档标注的「待 live-PG 校验」缺口；`hook_event_constraint_executes_against_live_schema`（manifest.rs）在真实 schema 上插入 `metadata.provider.query` hook 证明迁移 0078 的动态 drop+re-add 约束接受新事件、并断言未知 `event_key` 被 DB CHECK 拒绝（此前仅有静态字符串守卫），经 plugin package cascade 自清理。两条 `#[ignore]` 冒烟本地实跑通过。
- STRM、下载、转码和媒体探测已有基础安全边界。
- 库级图片语言覆盖已落地（设计 `metadata-scraper-design.md` §7.2 此前推迟的「库级可选覆盖」）：新增 additive 迁移 `0079_library_image_language.sql`，给 `libraries` 加 `preferred_image_language` / `preferred_image_prefer_original` / `preferred_image_fallback_languages` 三列（带 locale 形态 check，NULL 表示继承全局/provider 默认），不动现有行与约束。`metadata.refresh` worker 的 `load_target` SQL 已读取这三列并写入 `MetadataTarget`，`build_lookup` 实现「库级 ← 全局 ← provider 内置」的图片语言优先级折叠（库级 Some 覆盖全局，空 fallback 列表不会遮蔽全局回退），与文本语言层解耦。新增 3 个 `build_lookup` 优先级单测 + SQL 守卫单测；新增 live-PG 冒烟 `metadata_load_target_executes_against_live_schema` 在迁移后真实 schema 上 seed 库（带图片语言覆盖）+ 媒体项，经生产 `load_target` 读回并断言库级覆盖压过冲突的全局默认，自清理。这同时关闭此前核心 `metadata.refresh` worker `load_target` SQL 无 live-PG 冒烟的缺口（此前仅编译期 + 字符串守卫）。
- 媒体库管理生命周期补齐读回与更新（此前只能 `POST` 创建，无法列出或修改）：新增 `GET /api/admin/libraries` 按 `(name, id)` keyset 分页列出媒体库及完整设置（语言/区域、图片语言策略、隐藏状态），支持 `libraryType` / `isHidden` / `cursor` / `limit`，`limit + 1` lower-bound 计数避免大部署精确全表扫描，翻页走 `x-fbz-has-more` / `x-fbz-next-cursor`；新增 `POST /api/admin/libraries/{id}/settings` 整体替换库设置，按 `public_id` 做索引友好的 uuid-safe 比较，未命中返回 404。两个新 handler 均经 `every_admin_route_handler_enforces_server_admin` 守卫强制 server-admin 门控，新增 `LibrarySettingsRecord` / `LibraryListFilter` / `LibraryListPage` 与独立 DTO（不触碰 `create_library` 的 RETURNING 链路）。新增 `libraries_list_limit` 钳制单测 + 两条 live-PG 冒烟 `library_settings_list_executes_against_live_schema`（建库→keyset 列表读回→cursor 翻页推进）与 `library_settings_update_executes_against_live_schema`（建库→更新往返→不存在 id 返回 None），均 seed 角色+owner+库并自清理。
- 媒体库管理生命周期进一步补齐路径增删切换与删库（此前路径只能 `POST` 添加、库创建后无法删除）：新增 `GET /api/admin/libraries/{id}/paths`（列出路径+启用状态，路径数小不分页）、`DELETE /api/admin/libraries/{id}/paths/{pathId}`（删除单路径）、`POST /api/admin/libraries/{id}/paths/{pathId}/settings`（启用/禁用单路径）和 `DELETE /api/admin/libraries/{id}`（删库，FK 级联清理路径+权限）。路径写操作按数字 `pathId` 但全部用 `library_paths.library_id = libraries.id and libraries.public_id = case ... $1::uuid` 限定在目标库 `public_id` 范围内（防跨库越权），删库按 `public_id` 做索引友好 uuid-safe 比较；非数字/非正 `pathId` 在 handler 层直接判 404 不进 SQL。新增 `LibraryPathSelector` / `SetLibraryPathEnabledInput` 输入结构和 `remove_library_path` / `set_library_path_enabled` / `list_library_paths` / `delete_library` 仓储方法，4 个新 handler 均经 `every_admin_route_handler_enforces_server_admin` 守卫；新增 live-PG 冒烟 `library_path_lifecycle_executes_against_live_schema` 端到端验证 add→list→toggle→cross-library-scope-isolation→remove→delete-cascade，自清理。
- 元数据 provider test 探测链路补 live-PG 冒烟：`POST /api/admin/metadata/providers/{id}/test` 在探测前执行 `resolve(cipher)` + `resolve_metadata_config` 折叠出 effective config 再交给联网 `probe_provider`；新增 `metadata_provider_test_resolve_executes_against_live_schema` 在真实 schema 上 seed tmdb override + 加密 key，验证 resolve+fold 后 DB 覆盖（base URL）和解密 token 压过 env 基线（即 probe 实际收到的输入），不触发网络调用，自清理并恢复 dev 既有 tmdb 配置。
- 媒体类型分类法根治（设计 `docs/plans/media-type-taxonomy-design.md`）：把此前 library_type(4 值) / item_type(9 值) / 前端三套互不一致的分类法收敛为单一事实源 `src/media_types.rs`（`LibraryType` + `ItemType` 枚举 + `MediaCategory` + 扩展名词典 + `resolution_from_height`）。`library_type` 对齐 Emby CollectionType 词汇（`movies/tvshows/music/homevideos/mixed/livetv`，改源头迁移 0002）、`item_type` 补 `photo/video/tvchannel/program/recording`；DB CHECK、admin 校验（`validate_library_type`）、扫描分类（`ItemType::classify`）、Emby content_types 与 `views/media_folders/items` 的 `library_type → collection_type` 映射全部从枚举派生，消除 `tv` vs `tvshows` 透传漂移；Emby items 浏览补 photo/video/直播三类双向 Type/MediaType 映射。`livetv` 仅占类型位（频道/EPG/录制子系统未做）。
- 家庭库图片全量能力：迁移 0080 `media_photo_metadata`（EXIF/尺寸/GPS/缩略图，captured_at 时间线索引）；`media/photo.rs` 提取模块（尺寸 + EXIF 纯函数，含 GPS 度分秒/曝光分数换算）+ photo worker（认领 `media.photo` job → 提取 → 等比缩放生成 JPEG 缩略图落 artwork_cache_dir → upsert，并同事务把 captured_at 写回 `media_items.premiere_date`/`production_year`，NULL 保留原值）；扫描把 photo 条目分流到 photo 队列（不喂 ffprobe）。admin API：`GET /api/admin/libraries/{id}/photos`（时间线 keyset）+ `GET /api/admin/media-items/{id}/thumbnail`（鉴权缩略图字节，内部 bigint id 拼路径防穿越）。Emby 暴露：photo 条目合成 Primary image tag + `Items/{id}/Images/Primary` 在 artwork 未命中时回退读 photo-thumbnails，客户端可渲染照片。前端 `components/admin/AdminPhotos.vue` 相册视图（blob 缩略图 + EXIF 摘要 + keyset 加载更多）。worker 受 `FBZ_PHOTO_WORKER_ENABLED`/`_INTERVAL_SECONDS` + 节点角色门控。
- 媒体识别子系统（MoviePilot 形态文件名识别，设计 `docs/plans/media-recognition-design.md` 阶段 0-5 全部落地）：新模块 `src/recognition/`，是 §3「媒体库扫描和元数据入库」缺失的**上游识别层**（净室重写，不参照 GPL 源码）。`recognize(input, library_type, &RuleSet)` 纯函数管线覆盖 Scene 命名、SxxExx、1x05、裸 E08、中文季集（廿/两/零）、动漫方括号 `[01]`/v2、specials、分卷、目录证据合并、置信度退化（Low→裸文件名，零回归）。自定义识别词引擎（`rules.rs`）四类规则 block/replace/offset/replace_offset + 偏移表达式求值（常量隐式 `EP±N` / 线性 `EP*2-1`，越界保留原集号）+ RuleSet 编译（坏正则跳过不阻断）。迁移 0082 `recognition_words`（pattern CHECK 对 offset 类放宽空前定位词）+ `repository.rs` CRUD/加载编译 + admin API `/api/admin/recognition/words`（CRUD）与 `/recognition/test`（不扫库验证规则）。扫描集成（`scan/service.rs`）：`recognize_scan_file` 填 title/year/season/episode/item_type，`ensure_episode_parent` find-or-create series→season 容器填 parent_id（迁移 0083 部分唯一索引保证并发去重，live-PG 验证拓扑）。迁移 0084 给 `media_files` 加画质列（resolution/source/video_codec/audio_codec/hdr），扫描时由文件名识别填入、probe 跑完用 ffprobe 实测视频流分辨率/编码校正（实测优先，coalesce 缺失保留）。provider 契约：`MetadataLookup` 加 season/episode/original_title 字段（`build_lookup` 从 media_items 读填）下发 provider 查剧集元数据。识别核心 + 规则引擎纯函数穷举单测全绿，迁移与扫描归组经 live-PG 实跑验证。
- 元数据 provider 健壮性：`MetadataSettingsRepository::resolve` 此前单行无法解密的 provider 密文（密钥轮换未重新加密 / 单行损坏）会让整个 provider 解析崩溃；已改为坏行跳过 + warn，只让该 provider 退化为无 key，其余正常（与仓库 skip-and-warn 哲学一致）。

## 还需要继续执行

### 1. Emby 协议兼容补齐

- 对照官方 Emby REST API 和真实客户端请求继续补缺口。
- 优先覆盖客户端启动、媒体库浏览、音乐播放、播放控制、PlaybackInfo、字幕、图片和用户数据高频接口。
- 每个新增兼容入口都要加路由存在测试和 DTO/查询映射测试。

### 2. 播放和转码生产化

- 完善 HLS 转码 session 生命周期、取消、过期清理、输出目录清理和错误审计。
- 继续验证硬件转码参数、失败回退软件转码、3 并发排队策略。
- 对 STRM 302 安全域名、内网识别和下载权限做更多边界测试。

### 3. 媒体库扫描和元数据入库

- 文件名识别层已落地（见上「媒体识别子系统」，design 阶段 0-5）：识别核心 + 自定义识别词 + 扫描集成 + 层级归组 + QualityTags + provider 契约扩展均已完成并 live-PG 验证。
- provider 联网编排已生产化（三块）：(1) **TMDB episode 下钻**——识别出 season/episode 后调 `/tv/{id}/season/{s}/episode/{e}` 取真正单集元数据（集标题/简介/播出日期/剧照）覆盖剧集级，best-effort 失败保留 series 级；(2) **重试退避**（`provider/retry.rs`）——429/5xx/超时/连接 = 瞬时有界重试 + 指数退避，其余 4xx 确定性失败不重试，纯决策函数可穷举单测，registry base-match 调用包重试；(3) **多 key 令牌池**（`provider/token_pool.rs`，可选）——一个 provider 可配多个 API key（admin `keys` 数组，最多 32），运行时轮转 + 429 标记冷却跳过，把限额翻倍；多 key 换行 join 存进现有密文行（不改 schema），resolve split 回池；对任意 provider 通用含 `plugin:{id}`；429 时配合 retry 自动轮转到下一个 key；(4) **响应缓存**（`metadata/cache.rs`）——相同逻辑条目（标题/年份/季集/语言）在 TTL（默认 6h）内复用上次结果跳过联网，正/负结果都缓存，缓存键是纯函数可单测。
- 继续完善大库扫描分片、missing 收敛、NAS 临时不可达保护。
- provider 仍待续：`PluginMetadataQuerier` 同步执行路径（design 标注待续）、enrich 阶段重试（原位富化的借用/正确性问题，有意跳过）、provider fallback 链的真实端到端（需真实 API key + 网络，本地到纯逻辑 + EXPLAIN 层）。
- 音乐 metadata、专辑、艺术家、曲目、封面和歌词/章节等后续需要单独补齐（当前歌词只读 sidecar、章节为兼容空、专辑/艺术家无真实 provider 富化）。

### 4. 数据库规模化

- 继续检查所有 Admin API、高增长 outbox、execution run、notification attempt、job/event 表是否 keyset 化。
- Admin scheduled task 列表 keyset 查询已补真实 schema 冒烟 `scheduled_task_admin_queries_execute_against_live_schema`：在迁移后的 PostgreSQL 上校验 scheduled-task admin、task_type 与 owner_type keyset 索引存在，并通过生产 repository 方法执行 task_type、owner_type、enabled、cursor 和非法 cursor 空结果，防止计划任务管理列表查询与迁移漂移。
- Admin user 列表 keyset 查询已补真实 schema 冒烟 `admin_user_list_queries_execute_against_live_schema`：在迁移后的 PostgreSQL 上校验 username、role/username、disabled/username 与 role/disabled/username keyset 索引存在，并通过生产 repository 方法执行 role、disabled、role+disabled、cursor 和非法 cursor 空结果，防止用户管理列表查询与迁移漂移。
- Admin 用户媒体库权限列表查询已补真实 schema 冒烟 `admin_user_library_permission_queries_execute_against_live_schema`：在迁移后的 PostgreSQL 上校验 library name 与 type/name keyset 索引存在，并通过生产 repository 方法执行 library_type、configured/unconfigured、cursor、隐藏库有效权限和 valid-missing user id no-op，防止用户权限管理列表查询与迁移漂移。
- 通知投递 worker 的 `event_outbox` claim 已覆盖 `notification.send.requested` 事件类型专用 partial indexes（pending/failed 可领取与 delivering 过期租约），保持与插件 hook dispatch claim 同级别的索引选择性。
- 通知 Admin 审计查询已补真实 schema 冒烟 `notification_admin_audit_queries_execute_against_live_schema`：在迁移后的 PostgreSQL 上校验通知请求 recent 索引与投递尝试 request/status keyset 索引存在，并通过生产 repository 方法执行请求列表的 channel/status/cursor 组合、投递尝试列表的 status/cursor 组合和非法 request id no-op，防止高增长 `plugin_notification_requests` / `notification_delivery_attempts` 查询与迁移漂移。
- Admin 通知目标列表查询已补真实 schema 冒烟 `notification_target_admin_queries_execute_against_live_schema`：在迁移后的 PostgreSQL 上校验 notification target 的 recent、enabled、channel 与 channel/enabled keyset 索引存在，并通过生产 repository 方法执行 target_type/channel/enabled 组合、cursor 和非法 cursor 空结果，防止通知目标管理列表查询与迁移漂移。
- Admin scheduled-task run 历史查询已补真实 schema 冒烟 `scheduled_task_run_history_queries_execute_against_live_schema`：在迁移后的 PostgreSQL 上校验 task recent 与 task/status keyset 索引存在，并通过生产 repository 方法执行 status/cursor、非法 cursor 和 missing task key no-op，防止分区后的高增长 `scheduled_task_runs` Admin 历史查询与迁移漂移。
- Admin job 列表查询已补真实 schema 冒烟 `admin_job_list_queries_execute_against_live_schema`：在迁移后的 PostgreSQL 上校验 job status 与 queue keyset 索引存在，并通过生产 repository 方法执行 status/type/queue 组合、cursor 和非法 cursor 空结果，防止高增长 `jobs` Admin 列表查询与迁移漂移。
- Admin job run/event 历史查询已补真实 schema 冒烟 `admin_job_run_event_queries_execute_against_live_schema`：在迁移后的 PostgreSQL 上校验 job run status keyset 索引与 job event level keyset 索引存在，并通过生产 repository 方法执行 job detail、run status/cursor、event level/cursor 和 valid-missing job id no-op，防止分区后的高增长 `job_runs` / `job_events` Admin 审计查询与迁移漂移。
- 插件派发 Admin 审计查询已补真实 schema 冒烟 `plugin_dispatch_admin_queries_execute_against_live_schema`：在迁移后的 PostgreSQL 上校验 plugin dispatch outbox status keyset 索引与 per-dispatch execution run status keyset 索引存在，并通过生产 repository 方法执行派发列表的 status/cursor 组合、执行记录列表的 status/cursor 组合和非法 dispatch id no-op，防止高增长 `event_outbox(plugin.hook.dispatch)` / `plugin_execution_runs` 查询与迁移漂移。
- Host API 调用审计已覆盖 `pluginId/statusCode` 和 `executionRunId/statusCode` 组合过滤索引，并补真实 schema 冒烟 `plugin_host_api_admin_queries_execute_against_live_schema`：在迁移后的 PostgreSQL 上校验 recent、plugin/status 与 execution/status keyset 索引存在，并通过生产 repository 方法执行 plugin/status、execution/status、cursor、per-run 列表、token/run join 和非法 execution run id no-op，防止高增长 `plugin_host_api_calls` 审计查询与迁移漂移。
- 针对 5PB / 百万级媒体量继续补复合索引、partial index、查询上限、批处理上限。
- 分区/归档设计已固化:`docs/database-partitioning-design.md` 给出高增长表(`job_events`、`event_outbox`、`job_runs`、`jobs`、`scheduled_task_runs`、`plugin_host_api_calls`、`plugin_execution_runs`、`notification_delivery_attempts`、`plugin_notification_requests`、`playback_sessions`、`transcoding_sessions`)的按时间 RANGE 分区键、保留热窗口、归档(DETACH+归档+DROP)、冷热边界、物化统计(`queue_stats_rollup` 避免扫全分区)、唯一约束/外键改造和滚动维护方案,并明确分区为结构变更、落地前需确认。应用层 claim/readiness/admin keyset 查询谓词已是时间+状态,分区裁剪天然可用,基本无需改写。
- 分区首张表已落地:经确认后实现迁移 `0064_partition_job_events.sql`,把 `job_events` 按 `created_at` 月度 RANGE 分区(PK 改 `(id, created_at)`、复用 `job_events_id_seq`、保留 jobs/job_runs 外键与四个原名 keyset/时间索引、回填现有行、建 `2026m06/07/08` + `default` 分区)。本地 dockerized PostgreSQL 实跑校验:`success=t`、relkind `p`、28 行全部保留并落 `2026m06`、`2026-07-15` 插入路由 `2026m07` 且 id 续 29、legacy 表已删;`record_job_event` 的 INSERT 未变,透明路由。`jobs.rs` 加迁移结构测试 `job_events_partition_migration_partitions_by_created_at`。
- 分区第二张表已落地(category B 首张):迁移 `0065_partition_plugin_host_api_calls.sql` 把高增长审计表 `plugin_host_api_calls` 按 `finished_at` 月度 RANGE 分区,验证并实现 `public_id` UNIQUE → 普通索引降级(随机 uuid + 审计 INSERT 无 ON CONFLICT,降级安全),保留 11 个原审计索引(含预算上限索引)与三出站外键。本地实跑校验:`success=t`、relkind `p`、11 行保留、无 UNIQUE 约束且 public_id 索引非唯一、预算索引在、`2026-08-10` 插入路由 `2026m08`、legacy 已删。`jobs.rs` 加 `plugin_host_api_calls_partition_migration_relaxes_public_id_and_partitions_by_finished_at` 测试。- 分区第三张表已落地(category B):迁移 `0066_partition_scheduled_task_runs.sql` 把调度运行历史 `scheduled_task_runs` 按 `started_at` 月度 RANGE 分区,`public_id` UNIQUE → 普通索引,保留 active/task_recent/task_started/task_status 四索引与外键。虽有 running-lease/claim 语义,但 `started_at` 插入后不变(行不跨分区),已实测 active-query 兼容:插入 running run 路由 `2026m06`、按 id 更新状态成功、lease 回收查询正确。`success=t`、relkind `p`、2 行保留、active partial 索引在。`jobs.rs` 加 `scheduled_task_runs_partition_migration_partitions_by_started_at` 测试。
- 分区第四张表已落地(category C 首张):迁移 `0067_partition_job_runs.sql` 先 drop 入站 FK `job_events_job_run_id_fkey`(安全分析:job_runs 仅经 jobs 级联删除,该级联同时删除引用方 job_events,SET NULL 路径永不触发;`job_run_id` 列保留),再把 `job_runs` 按 `started_at` 月度 RANGE 分区(无 public_id,无需唯一性降级)。本地实跑校验:`success=t`、relkind `p`、14 行保留、入站 FK 已删且列保留、`2026-08-20` 插入路由 `2026m08`、legacy 已删。`jobs.rs` 加 `job_runs_partition_migration_drops_inbound_fk_and_partitions_by_started_at` 测试。
- 至此 A(`job_events`)+ B(`plugin_host_api_calls`、`scheduled_task_runs`)+ C(`job_runs`、`playback_sessions`)共五表已实现并实跑校验,四种模式(纯净 / public_id 降级 / 入站 FK 安全 drop / 入站 SET NULL 触发器替代)均已证。分区滚动维护机制已落地:迁移 `0068` 建 `ensure_partition_coverage(months_ahead int) returns int`,先幂等为四张已分区表创建当前月 + N 个未来月分区(已存在跳过),并在迁移内调用 `(18)` 把覆盖延伸到 2027m12(各表 19 月分区 + default);迁移 `0069` 又把 `playback_sessions` 加入同一函数并再次调用 `(18)`。本地实跑校验:函数存在、五表各 20 分区、重复调用返回 0(幂等)。`jobs.rs` 加 `partition_coverage_function_covers_all_partitioned_tables_idempotently` 测试,并守卫 `0069` 后文档必须记录 `playback_sessions` 已纳入覆盖。滚动计划任务已落地:`core.partition.maintenance`(task_type `partition.maintenance`,`SCHEDULE_PARTITION_MAINTENANCE` 默认 `daily`)在 `bootstrap_core_tasks` 注册、`run_claimed_task` 调 `ensure_partition_coverage(6)` 保持向前 6 月覆盖,受 scheduler 角色+开关门控,并调用 `refresh_queue_stats_rollup_for_month("today")` 刷新当前月热 bucket。`queue_stats_rollup` 物化统计基础表已由迁移 `0076` 落地,主键为 `(bucket_date, table_name, status)`,计数字段为 `row_count`,并有按 `table_name/bucket_date/status` 读取的索引;调度仓储新增非破坏性月度刷新,按五张已分区表写入 `job_events.event_level`、`plugin_host_api_calls.status_code`、`scheduled_task_runs.status`、`job_runs.status` 和 `playback_sessions` active/stopped 计数,`on conflict` 幂等更新且不 `DETACH`/`DROP`。冷分区归档候选发现基础已落地:`list_partition_archive_candidates(retention_months, limit)` 只读枚举 `pg_inherits`,排除 default 分区,只返回早于热窗口且已有 `queue_stats_rollup.source_partition` 证据的月度分区候选,不执行 DDL。Admin 只读可观测入口已落地:`GET /api/admin/partition-maintenance/rollups` 按 allowlist 表名、bucket 日期范围和 limit 读取 `queue_stats_rollup`;`GET /api/admin/partition-maintenance/archive-candidates` 按 retention/month limit 读取候选。两者都要求 server admin,只调用调度仓储只读查询,不执行 `DETACH`、`DROP`、`DELETE` 或归档写动作。结构守卫覆盖刷新 SQL、rollup 读取 SQL、候选发现 SQL 与调度/Admin 接线,真实 schema 冒烟 `queue_stats_rollup_refresh_writes_against_live_schema`、`queue_stats_rollup_read_query_executes_against_live_schema`、`partition_archive_candidates_plan_against_live_schema` 已实跑通过(计划、写入/读取/发现、重复刷新不累加、当前月不误判为归档候选)。待办:真正冷分区归档/`DETACH`+`DROP` 执行任务仍为设计,落地前需单独确认。
- C 类逐张安全性分析后结论:`job_runs` 是唯一可简单安全 drop 的 C 表(已落地);`plugin_execution_runs` 入站 FK 虽可安全 drop,但另带业务唯一 `(outbox_event_public_id, attempt)`(派发幂等不变量,DB 强制),分区将被迫移除该不变量,需单独决策,暂不分区;`playback_sessions` 的入站 SET NULL 路径会被触发(经 users/media_items 级联独立删除),已用**触发器方案**落地(迁移 `0069`):先建 `BEFORE DELETE` 触发器复刻 SET NULL(置空 transcoding_sessions 引用)+ 支撑索引,再 drop 入站 FK,再按 `started_at` 分区(public_id 降级),并加入 `ensure_partition_coverage`。本地实跑校验含触发器行为实测(删除 playback_session 后 transcoding 引用被自动置空)。`notification_delivery_attempts` 经复核除 `public_id` 唯一外还有 `idx_notification_delivery_attempts_target_success` partial unique index,与 delivery worker 的 `has_successful_attempt` 前置检查共同保护「同一通知请求同一目标最多一个成功投递」;按 `created_at` 分区会迫使该成功幂等约束降级或变成分区内唯一,因此暂不分区,未来必须先设计请求级成功状态/幂等键等替代防线。`plugin_notification_requests` 也暂不分区:它被 `notification_delivery_attempts.notification_request_id` CASCADE 引用,分区后单列 `id` 不能继续作为 FK 目标,且 request/attempt 必须与 attempts 成功幂等阻塞点一起重设计。C 余两表如上各有阻塞;D 类活跃队列(`jobs`/`event_outbox`/`transcoding_sessions`,最高风险,最后)。`database-partitioning-design.md` 已补「各表落地就绪分析」:基于实跑 schema 巡检把候选表分为 A(无 public_id 的纯 leaf,仅 job_events,已 done)、B(leaf 但有 `public_id uuid` 唯一约束,需先定唯一性降级:`plugin_host_api_calls`/`scheduled_task_runs`)、B+业务唯一阻塞(`notification_delivery_attempts` 暂不分区)、C(有入站外键需先改造引用方:`job_runs`←job_events、`plugin_notification_requests`←notification_delivery_attempts、`plugin_execution_runs`←host_tokens/host_api_calls、`playback_sessions`←transcoding_sessions)、D(活跃队列 `jobs`/`event_outbox`/`transcoding_sessions`,最后单独评估),给出 A→B→C→D 的实施顺序与每表具体阻塞点。
- SQL 改动要验证查询形态，避免 `public_id::text = any(...)` 这类破坏索引的写法（现有代码已无此写法，且 `library/repository.rs` 有守卫测试断言主查询不含该模式）。本轮也已收敛 repository 中少一段的 UUID regex guard（`8-4-4-12`），统一为标准 `8-4-4-4-12`，避免 valid public id 在进入索引友好的 `public_id = case ... then $n::uuid` 比较前被误判为空；新增跨 repository 源码守卫 `public_id_sql_uuid_guards_use_canonical_uuid_shape`。

### 5. 插件生态生产闭环

- 插件 smoke 已覆盖 signed package、Host API budget、通知投递、计划任务声明同步和计划任务派发执行；后续继续补真实插件模板和 WASI 模板时保持同样的端到端 smoke 标准。
- WASI 插件模板已补：首个一方 WASI 示例 `examples/plugins/wasi-scan-logger-template/`（独立 workspace 的 wasm crate，`manifest.json` + `Cargo.toml` + `src/main.rs` + `README.md`），演示 WASIp1 沙箱纯计算契约(argv/env/stdin→stdout、`/plugin`/`/data`/`/cache`/`/tmp` 预挂载、fuel/内存/epoch/stdio/模块大小上限、无网络)。两层校验：`manifest.rs::first_party_wasi_scan_logger_manifest_is_valid` 用真实校验器验证 manifest(随 `cargo test`)；`wasi.rs::wasi_scan_logger_template_executes_end_to_end` 把编译出的 `plugin.wasm` 经真实 `PluginWasiRuntime::execute` 跑通 stdin→stdout(标 `#[ignore]` 避免默认 `cargo test` 依赖 `wasm32-wasip1` 目标,已本地实跑通过)。`docs/plugin-development.md` 补「WASI 运行时契约与模板」章节。
- WASI 插件用于沙箱纯计算;联网 / Host API 插件继续优先 HTTP runtime(WASIp1 无 socket)。

### 6. 多用户和管理权限

- 管理 API 服务器管理权限已审计：`/api/admin/*` 无统一中间件，靠每个 handler 调用 `authenticate_admin`（内部校验 `can_manage_server`）；审计确认全部 handler 都强制该门控（`enable/disable_notification_target` 经 `set_notification_target_enabled` 间接校验）。已加回归守卫测试 `every_admin_route_handler_enforces_server_admin`，扫描 `routes.rs` 确保新增 admin handler 不会漏掉权限门控。
- 继续补更细的 Emby 用户策略字段和客户端真实行为映射，尤其真实频道、标签、设备和会话限制模型落地后的端到端策略一致性。
- 媒体库权限、下载、转码、新设备登录、会话撤销需要保持端到端一致。

### 7. 运行态可靠性和可观测

- `/ready` 后续可继续按节点职责扩展 worker 健康摘要；worker 开关、scheduler due/running/expired run 摘要、通用队列 backlog、事件镜像 backlog、队列消费归属、当前节点 assigned backlog 和 expired lease 摘要已有基础输出。
- 增加尚未覆盖的 worker 租约过期和其他队列重试的结构化日志；慢 HTTP、慢 SQL、通用 job lease、计划任务 run lease、转码 lease、插件 execution run lease、事件镜像 lease 回收、通用 job handler 失败重试（scan/metadata/probe）、转码失败、事件镜像重试、插件 dispatch 重试和通知投递重试已有基础结构化日志。
- 已补按节点职责的队列归属：`/ready` 各队列 backlog 现带 `drained_by_node` 和 `expired_leases`，并用顶层 `assigned_backlog` / `has_assigned_backlog`、`assigned_expired_leases` / `has_assigned_expired_leases` 汇总当前节点实际负责消化或接管的积压；后续可继续按节点职责裁剪 worker 健康摘要的展示粒度。

### 8. 部署和运维

- 已补 `docs/deployment.md` 生产部署与运维指南：节点拓扑与 worker 双重门控、前置依赖（PG16/Redis7/FFmpeg）、
  环境变量生产关键项、持久化目录与卷挂载、FFmpeg/ffprobe 覆盖优先级、多阶段 Dockerfile 示例、
  单机 all 与多节点拆分的生产 docker-compose 示例、NAS 注意事项（媒体只读/路径一致/临时不可达重试/STRM 安全/资源约束）、
  `/health`+`/ready` 探针与结构化日志、10 步运维恢复检查清单；README 已加「生产部署」入口。
- 已补反向代理 / TLS 终止指引（nginx 关闭流式 buffering、放宽超时与 body size、`PUBLIC_BASE_URL` 对齐）、备份与恢复策略（PostgreSQL 为唯一权威状态、Redis 可重建、派生缓存可丢弃、恢复顺序）和大表迁移上线锁说明（启动期 `CREATE INDEX` 非并发会短暂持写锁，建议低峰单节点先迁移再扩容）。
- 后续可继续补：真实镜像 CI 构建与发布、按真实硬件平台的硬件转码设备透传细则、备份恢复演练脚本。

## 当前建议的下一轮任务

优先级建议：

1. Emby 兼容：继续补真实客户端高频缺口，尤其音乐播放和播放控制。
2. 数据库规模化：继续审计剩余 Admin API 和高增长表的分页/索引形态。
3. 运行态可靠性：继续补尚未覆盖的 worker 租约过期和队列重试结构化日志。
4. 部署和运维：补生产 Docker/NAS 部署说明、卷挂载和本地依赖恢复脚本。

每一轮只选一个方向推进，完成后更新本文或相关文档。
